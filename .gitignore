# ── Environment & Secrets ─────────────────────────────────────
.env
.env.local
.env.*.local
.env.development
.env.production

# ── Dependencies ──────────────────────────────────────────────
node_modules/

# ── Build output (assets generated by Vite) ───────────────────
# NOTE: dist/index.html, dist/CNAME, dist/sw.js, dist/manifest.json,
# dist/admin/, dist/icons/, dist/public files ARE tracked because
# GitHub Pages deploys from dist/ via CI (see .github/workflows/main.yml).
dist/assets/

# ── Supabase CLI temp files ────────────────────────────────────
supabase/.temp/

# ── Editor & OS ───────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
Thumbs.db
*.swp
*.swo
.idea/
.vscode/settings.json

# ── Vite / Wrangler cache ─────────────────────────────────────
.vite/
.wrangler/
node_modules/.cache/

# ── Logs ──────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── Claude Code ───────────────────────────────────────────────
# Track skills + project instructions — useful on any machine
# Ignore local permission settings (machine-specific)
.claude/settings.local.json
.claude/worktrees/
