# Agent Iteration Log

Each row is one `/iterate` run. Token costs are for the full Claude Code session — run `/cost` at the end of a session to get the number, then note it in the Session Cost column.

| Date | Version | Area | Change | Files | Session Cost |
| 2026-06-23 | v1.46.443 | UX | Login simplified to OAuth-only (GitHub + Google) — removed email/password and magic-link forms | `LoginPage.jsx:31-91` | — |
| 2026-06-23 | v1.46.444 | Fix | Pruned dead email/magic-link auth functions from supabase.js and useAuth.jsx | `supabase.js:25-53`, `useAuth.jsx:2,18` | — |
| 2026-06-24 | v1.46.447 | Fix | Podcast player loading spinner now appears immediately on play — eliminated frozen gap before browser fires `waiting` | `PodcastPlayer.jsx:268,341-345,362` | — |
| 2026-06-24 | v1.46.448 | Polish | Unified Saved bookmark icon between Sidebar and BottomNav — same shape on desktop and mobile | `Sidebar.jsx:13` | — |
| 2026-06-25 | v1.46.449 | Nav | Cards moved before Review in Sidebar + BottomNav — matches read→highlight→Cards→Review journey | `Sidebar.jsx:446-447`, `BottomNav.jsx:50-51` | — |
| 2026-06-26 | v1.46.450 | Perf | Saved page stale-while-revalidate localStorage cache + exclude search_vector from query | `ReadLaterPage.jsx:54-67`, `supabase.js:234-240` | — |
| 2026-06-26 | v1.46.451 | Perf | Cards page stale-while-revalidate localStorage cache for highlights + review state | `CardsPage.jsx:152-177` | — |
| 2026-06-26 | v1.46.452 | Polish | Today page Queue shows skeletons during load instead of false "No cards due"/"Nothing saved" | `TodayPage.jsx:73-74,340-354` | — |
| 2026-06-26 | v1.46.453 | Perf | Review page reads from shared highlights/review cache (written by Cards) — session starts instantly | `ReviewPage.jsx:121-167` | — |
| 2026-06-26 | v1.46.454 | Polish | Replaced emoji empty-state icons with theme-aware SVGs in Inbox and Cards pages | `InboxPage.jsx:1364`, `CardsPage.jsx:645` | — |
| 2026-06-26 | v1.46.455 | UX | Bottom nav icon pill active state + labels 9→10px + badge offset fix | `BottomNav.jsx:54-177` | — |
| 2026-06-26 | v1.46.456 | UX | Bottom nav: icon-only (no labels), icon 20→22px, pill padding 3→6px vertical | `BottomNav.jsx:116-177` | — |
| 2026-06-26 | v1.46.457 | Perf | TodayPage Queue pre-populates reviewDue + savedItems from shared caches before Supabase fetch | `TodayPage.jsx:81-95` | — |
| 2026-06-26 | v1.46.458 | Polish | Replace 📡 📁 emoji empty states in Manage Feeds with SVGs | `SecondaryPages.jsx:1458,1498` | — |
| 2026-06-26 | v1.46.459 | Polish | Replace ✍️ 🕑 ⚠️ emoji in Highlights drawer, History, ErrorBoundary with SVGs | `HighlightsDrawer.jsx:67`, `SecondaryPages.jsx:59`, `UI.jsx:126` | — |
| 2026-06-26 | v1.46.460 | Polish | Replace ⚠️ 📡 emoji in ContentViewer article-error + RSS-fallback states with SVGs | `ContentViewer.jsx:694,768` | — |
| 2026-06-26 | v1.46.461 | Polish | Replace 🏷️ ✍️ 📝 Notes tab empty-state icons and 😵‍💫 ErrorBoundary with SVGs | `NotesPage.jsx:790`, `ErrorBoundary.jsx:48` | — |
| 2026-06-26 | v1.46.462 | Nav | Sidebar nav item active state now uses icon-pill design to match BottomNav | `Sidebar.jsx:56-71` | — |
| 2026-06-26 | v1.46.463 | Polish | Remove last functional 🔖 emoji from AddModal Save button | `AddModal.jsx:423` | — |
| 2026-06-26 | v1.46.464 | Fix | Add keyboard focus outlines to Inbox filter toggle buttons for WCAG accessibility | `InboxPage.jsx:1086-1093` | — |
| 2026-06-26 | v1.46.465 | Polish | Standardize "Mark all read" button border radius 9→8px for consistency | `InboxPage.jsx:1103` | — |
| 2026-06-26 | v1.46.466 | Security | Fix XSS vulnerability (remove dangerouslySetInnerHTML) + fix 26 npm audit CVEs | `ContentViewer.jsx`, `package.json` | — |
| 2026-06-26 | v1.46.467 | Security | Add feed URL validation + clarify API key docs (server-side only) | `supabase.js:45`, `.env.example` | — |
| 2026-06-15 | v1.46.442 | UX | Removed History nav + Catch-Up smart filter for minimal nav | `App.jsx:25,80,239,243`, `BottomNav.jsx:137`, `Sidebar.jsx` | — |
| 2026-06-15 | v1.46.441 | Fix | Highlighting: yellow ::selection CSS all article modes; toolbar flips below selection if near top; header+scroll reset on article switch | `index.css:173`, `ContentViewer.jsx:186,799`, `SelectionToolbar.jsx:34,109` | — |
| 2026-06-15 | v1.46.440 | Perf/Fix | Pre-compute _ts sort key in mergeAndSort; folder filter Set; analytics static import removes build warning | `InboxPage.jsx:246-270,362`, `analytics.js:12-52` | — |
| 2026-06-14 | v1.46.439 | Feature | iPad layout: sidebar expands with labels+feed list (180px) on tablet; split-pane list narrows to 280px when article open | `Sidebar.jsx:430-431`, `InboxPage.jsx:918` | — |
| 2026-06-14 | v1.46.438 | UX | Remove decorative borderBottom from inbox item rows — padding + hover bg sufficient separation | `InboxPage.jsx:1412` | — |
| 2026-06-12 | v1.46.437 | Feature | Offline reading: IndexedDB article cache persists across sessions; pre-caching silently fetches recent articles after feed load; reader auto-falls-back to cache when offline | `offlineCache.js` (new), `fetchers.js:1,399,568`, `InboxPage.jsx:7,305`, `main.jsx:7,10` | — |
| 2026-06-11 | v1.46.436 | Feature | Smart sort: keyword interest profile from highlight history; "Smart" toolbar toggle re-ranks inbox items by relevance; cached in localStorage | `InboxPage.jsx:6,117,175,380,1044` | — |
| 2026-06-11 | v1.46.435 | Feature | Image highlighting: click images in reader to save as highlight; [IMAGE]: prefix in passage; thumbnails in HighlightsDrawer and CardsPage | `ContentViewer.jsx:77,215,799`, `HighlightsDrawer.jsx:72`, `CardsPage.jsx:737,757`, `index.css:161` | — |
| 2026-06-11 | v1.46.434 | Feature | Chat with highlights: Ask input in theme view on CardsPage sends all highlights as context to Haiku; answer rendered inline | `CardsPage.jsx:11,57,135,310` | — |
| 2026-06-11 | v1.46.433 | Feature | Article translation: "Translate to English" in overflow menu; Haiku translates body; toggle pills switch between original and translated | `ContentViewer.jsx:6,55,332`, `fetchers.js:868` | — |
| 2026-06-11 | v1.46.432 | Feature | Bulk select: long-press any feed item to enter multi-select mode; tap items to select; action bar offers Mark read / Save all / Cancel | `FeedItem.jsx:293,488,648`, `InboxPage.jsx:109,639` | — |
| 2026-06-11 | v1.46.431 | Feature | Inline lookup: "?" button in SelectionToolbar calls Haiku to explain any selected text in 2 sentences | `SelectionToolbar.jsx:1`, `fetchers.js:854` | — |
| 2026-06-11 | v1.46.430 | UX | Catch-up swipe-to-dismiss: fly-off animation on swipe-right in Catch-Up mode on mobile | `FeedItem.jsx:103,145`, `InboxPage.jsx:1318` | — |
| 2026-06-11 | v1.46.429 | Feature | AI tag suggestions in NotePanel: Haiku suggests up to 5 theme tags per highlight passage; one-click to apply | `NotePanel.jsx:1,13` | — |
| 2026-06-11 | v1.46.428 | Feature | Reading activity heatmap: 12-week GitHub-style calendar grid on Stats page, color-coded by daily article count | `SecondaryPages.jsx:112` | — |
| 2026-06-11 | v1.46.427 | Feature | Feed health indicator: ⚠ dormant badge on feeds with no posts in 60+ days in ManageFeeds | `SecondaryPages.jsx:929` | — |
| 2026-06-11 | v1.46.426 | Feature | Obsidian export: "Copy for Obsidian" menu item outputs YAML frontmatter + [[wikilink]] tags from highlights | `exportUtils.js:69`, `ContentViewer.jsx:19,319` | — |
| 2026-06-11 | v1.46.424 | Feature | Muted keywords filter: hide articles matching muted terms from all feeds; Settings > Muted Keywords card to add/remove | `InboxPage.jsx:91,341`, `SecondaryPages.jsx:488` | — |
| 2026-06-10 | v1.46.423 | UX | ManageFeeds tab + filter type buttons: aria-pressed for screen reader state announcements | `SecondaryPages.jsx:1234,1282` | — |
| 2026-06-10 | v1.46.422 | UX | InboxPage display sheet: aria-pressed on image-position (L/R/None) and view-mode (List/Cards) buttons | `InboxPage.jsx:1345,1084` | — |
| 2026-06-10 | v1.46.421 | UX | Clear search buttons: aria-label="Clear search" in SearchBar and CardsPage | `SearchBar.jsx:112`, `CardsPage.jsx:365` | — |
| 2026-06-10 | v1.46.420 | UX | InboxPage card-size buttons: aria-label + aria-pressed; Settings theme buttons: aria-pressed | `InboxPage.jsx:1099`, `SecondaryPages.jsx:557` | — |
| 2026-06-10 | v1.46.419 | UX | InboxPage feed list buttons: aria-labels on move-to-folder, show-error, remove-feed | `InboxPage.jsx:1758,1766,1770` | — |
| 2026-06-10 | v1.46.418 | UX | TagsInput × remove button + NotesPage ✕ dismiss AI Distill: aria-labels added | `TagsInput.jsx:53`, `NotesPage.jsx:479` | — |
| 2026-06-10 | v1.46.417 | UX | NotesPage view mode toggle: aria-label + aria-pressed on grid/list buttons | `NotesPage.jsx:689` | — |
| 2026-06-10 | v1.46.416 | UX | Icon-only ×/✕ buttons: aria-labels on HighlightsDrawer tag remove, NotePanel tag remove, ArticleNotesPanel close | `HighlightsDrawer.jsx:97`, `NotePanel.jsx:76`, `ArticleNotesPanel.jsx:109` | — |
| 2026-06-10 | v1.46.415 | UX | Transcript highlight color swatches + cancel button: aria-labels added | `ContentViewer.jsx:1294,1303` | — |
| 2026-06-10 | v1.46.414 | UX | Podcast player buttons aria-labels: play/pause, skip ±15/30s, rate, sleep timer | `ContentViewer.jsx:1431,1506,1528,1535` | — |
| 2026-06-10 | v1.46.413 | Nav | Dynamic document.title on page navigation — screen readers announce page name | `App.jsx:79` | — |
| 2026-06-10 | v1.46.412 | UX | NotesPage sort select + ReadLaterPage URL input: final aria-label gaps closed | `NotesPage.jsx:680`, `ReadLaterPage.jsx:150` | — |
| 2026-06-10 | v1.46.411 | UX | CardsPage search/passage/annotation + ContentViewer ask + ReviewPage annotation: aria-label sweep complete | `CardsPage.jsx:245,349,389,411`, `ContentViewer.jsx:1011`, `ReviewPage.jsx:421` | — |
| 2026-06-10 | v1.46.410 | UX | HighlightsDrawer + ArticleNotesPanel: aria-label on theme tag input + note title/body | `HighlightsDrawer.jsx:101`, `ArticleNotesPanel.jsx:127,134` | — |
| 2026-06-10 | v1.46.409 | UX | NotesPage: aria-label on note-edit textarea, note title + body fields | `NotesPage.jsx:221,445,459` | — |
| 2026-06-10 | v1.46.408 | UX | SecondaryPages: aria-label on feed-limit select, digest time input, delete-confirm input | `SecondaryPages.jsx:596,459,1601` | — |
| 2026-06-10 | v1.46.407 | Perf | CardsPage delete button hover: opacity transition instead of padding-right — no layout reflow | `CardsPage.jsx:687` | — |
| 2026-06-10 | v1.46.406 | Perf | ReviewPage progress bar + InboxPage OPML bar + unread dot → transform:scaleX/scale | `ReviewPage.jsx:343`, `InboxPage.jsx:1129,1529` | — |
| 2026-06-10 | v1.46.405 | Perf | ContentViewer reading progress bar → transform:scaleX, eliminates layout reflow on scroll | `ContentViewer.jsx:386` | — |
| 2026-06-10 | v1.46.404 | Perf | PodcastPlayer SeekBar thumb drag expand → transform:scale instead of width/height | `PodcastPlayer.jsx:67-91,133` | — |
| 2026-06-10 | v1.46.403 | UX | MobileSearchOverlay: aria-label on search input | `MobileSearchOverlay.jsx:143` | — |
| 2026-06-10 | v1.46.402 | UX | Onboarding: role=dialog + aria-modal + aria-label on welcome modal | `Onboarding.jsx:58` | — |
| 2026-06-10 | v1.46.401 | UX | SmartFeedModal + FolderModal: htmlFor/id connecting labels to Name + Keywords inputs | `SmartFeedModal.jsx:77,91`, `FolderModal.jsx:55` | — |
| 2026-06-10 | v1.46.400 | UX | AddModal: aria-label on URL input, podcast search, feed nickname | `AddModal.jsx:262,348,387` | — |
| 2026-06-10 | v1.46.399 | UX | NotePanel: htmlFor/id connecting "Your note" + "Theme" labels to fields | `NotePanel.jsx:53,67` | — |
| 2026-06-10 | v1.46.398 | UX | SearchBar: aria-label + aria-haspopup + aria-expanded + aria-controls linking input to results listbox | `SearchBar.jsx:94` | — |
| 2026-06-10 | v1.46.397 | UX | TagsInput: aria-label + aria-autocomplete + aria-expanded on input — screen reader users can use tag autocomplete | `TagsInput.jsx:60` | — |
| 2026-06-10 | v1.46.396 | UX | Spinner: role=status + aria-label=Loading — screen readers announce loading states app-wide | `UI.jsx:71` | — |
| 2026-06-10 | v1.46.395 | Polish | index.css: global font-family:inherit for form elements — CSS baseline reset for buttons/inputs/selects | `index.css:104` | — |
| 2026-06-10 | v1.46.394 | Polish | useTheme: set --accent CSS variable — article body links/blockquotes/selection now use active theme color instead of blue | `useTheme.jsx:50` | — |
| 2026-06-10 | v1.46.393 | Perf | PodcastPlayer SeekBar: fill RAF loop converted from style.width to scaleX() — GPU-composited seek bar updates | `PodcastPlayer.jsx:43,73,82,132` | — |
| 2026-06-10 | v1.46.392 | Perf | PodcastPlayer: RAF loop changed from style.width to style.transform scaleX() — no layout recalculation every 500ms | `PodcastPlayer.jsx:333,529,650` | — |
| 2026-06-10 | v1.46.391 | Perf | OPMLImport + ContentViewer TTS: progress bars converted to transform:scaleX() — GPU-composited, no layout reflow | `OPMLImport.jsx:173, ContentViewer.jsx:1493` | — |
| 2026-06-10 | v1.46.390 | UX | AddModal + SmartFeedModal: aria-expanded on "Browse popular feeds" and "Pick feeds" disclosure buttons | `AddModal.jsx:434, SmartFeedModal.jsx:154` | — |
| 2026-06-10 | v1.46.389 | UX | ContentViewer overflow menu: aria-label+aria-expanded+aria-haspopup on trigger; role=menu/menuitem on items | `ContentViewer.jsx:1083,1095` | — |
| 2026-06-10 | v1.46.388 | UX | InboxPage source filter dropdown: aria-expanded + aria-haspopup on trigger; role=listbox + aria-selected on options | `InboxPage.jsx:924` | — |
| 2026-06-10 | v1.46.387 | UX | InboxPage mobile toolbar: aria-label on Search/Unread/Display icon buttons; aria-pressed on Unread toggle | `InboxPage.jsx:1111` | — |
| 2026-06-10 | v1.46.386 | UX | MobileFeedDrawer: aria-current on quick-nav buttons (History/Stats/Settings) — completes mobile nav accessibility | `MobileFeedDrawer.jsx:300` | — |
| 2026-06-10 | v1.46.385 | UX | InboxPage: aria-pressed on Unread/All filter segmented control — screen readers announce selected state | `InboxPage.jsx:972` | — |
| 2026-06-10 | v1.46.384 | Perf | index.html: preconnect to Google Fonts origins — speeds up editorial theme font loading | `index.html` | — |
| 2026-06-10 | v1.46.383 | UX | MobileFeedDrawer: aria-label+aria-current on FeedRow+FolderSection; aria-expanded on folder toggle | `MobileFeedDrawer.jsx:20,72,92` | — |
| 2026-06-10 | v1.46.382 | UX | Sidebar: aria-label on Edit Folder button | `Sidebar.jsx:310` | — |
| 2026-06-10 | v1.46.381 | UX | Sidebar: aria-label+aria-current on FeedRow and FolderSection nav buttons — complete screen reader coverage | `Sidebar.jsx:191,248,284` | — |
| 2026-06-10 | v1.46.380 | UX | BottomNav+Sidebar: aria-current on Feeds button and smart feed rows; aria-label on catch-up+smart feed buttons | `BottomNav.jsx:143`, `Sidebar.jsx:511,349` | — |
| 2026-06-10 | v1.46.379 | Nav | MobileFeedDrawer: add built-in Catch Up to smart feeds section — closes desktop/mobile nav parity gap | `MobileFeedDrawer.jsx:336` | — |
| 2026-06-10 | v1.46.378 | Nav | Sidebar: Stats link for all users, not just admins — closes mobile/desktop parity gap | `Sidebar.jsx:591` | — |
| 2026-06-10 | v1.46.377 | Nav | Sidebar: add History to desktop library nav — closes mobile/desktop navigation parity gap | `Sidebar.jsx:435` | — |
| 2026-06-10 | v1.46.376 | Perf | index.html: preconnect to Supabase origin — removes TCP/TLS latency on auth check fired every page load | `index.html` | — |
| 2026-06-10 | v1.46.375 | Perf | ContentViewer: lazy+async on all article body images via HTML post-processing; remaining favicon/artwork fixes | `ContentViewer.jsx:92`, `FeedItem.jsx:220`, `AddModal.jsx:298` | — |
| 2026-06-10 | v1.46.374 | Perf | FeedItem progress bars: transform:scaleX() replaces width animation — GPU-composited, no layout reflow | `FeedItem.jsx:231,273,537` | — |
| 2026-06-10 | v1.46.373 | Perf | Add loading=lazy decoding=async to all 17 favicon/thumbnail images across list contexts | `FeedItem.jsx:267,548`, `Sidebar.jsx:206`, `InboxPage.jsx:1733`, `MobileFeedDrawer.jsx:39`, `+9 more` | — |
| 2026-06-10 | v1.46.372 | UX | Sidebar+MobileFeedDrawer+InboxPage+SecondaryPages: aria-label on icon-only title-only buttons | `Sidebar.jsx:378`, `MobileFeedDrawer.jsx:111`, `InboxPage.jsx:1031`, `SecondaryPages.jsx:1107` | — |
| 2026-06-10 | v1.46.371 | UX | PodcastPlayer sleep timer buttons: aria-label on both mini+expanded variants | `PodcastPlayer.jsx:497,624` | — |
| 2026-06-10 | v1.46.370 | UX | PodcastPlayer SeekBar: update aria-valuenow in RAF loop — live playback position for screen readers | `PodcastPlayer.jsx:44` | — |
| 2026-06-10 | v1.46.369 | UX | InboxPage SourceItem: role=button+tabIndex+onKeyDown+aria-label+aria-pressed — desktop source list keyboard nav | `InboxPage.jsx:1703` | — |
| 2026-06-10 | v1.46.368 | UX | NotesPage tag spans: role=button+tabIndex+onKeyDown+aria-label — keyboard tag filter | `NotesPage.jsx:294` | — |
| 2026-06-10 | v1.46.367 | UX | MobileSearchOverlay result rows: role=button+tabIndex+onKeyDown+aria-label — keyboard search result selection | `MobileSearchOverlay.jsx:223` | — |
| 2026-06-10 | v1.46.366 | UX | PodcastPlayer SeekBar: role=slider+tabIndex+onKeyDown(±5s, Home/End) — keyboard podcast seeking | `PodcastPlayer.jsx:112` | — |
| 2026-06-10 | v1.46.365 | UX | CardsPage annotation editor: role=button+tabIndex+onKeyDown(Enter/F2) — keyboard annotation editing | `CardsPage.jsx:733` | — |
| 2026-06-10 | v1.46.364 | Perf | Remove redundant dynamic supabase.js imports in App.jsx + SecondaryPages.jsx — eliminates Vite mixed-import warning | `App.jsx:168`, `SecondaryPages.jsx:50` | — |
| 2026-06-10 | v1.46.363 | UX | ReviewPage passage card: role=button+tabIndex+onKeyDown — keyboard users can reveal note with Enter/Space | `ReviewPage.jsx:384` | — |
| 2026-06-10 | v1.46.362 | UX | ContentViewer highlight marks: role=button+tabIndex+onKeyDown+aria-label — keyboard users can interact with highlights | `ContentViewer.jsx:885` | — |
| 2026-06-10 | v1.46.361 | UX | FeedItem ListItem+CardItem: role=button+tabIndex+onKeyDown+aria-label — every inbox article keyboard-navigable | `FeedItem.jsx:302,378,491` | — |
| 2026-06-10 | v1.46.360 | UX | SecondaryPages EditableName: tabIndex+role=button+onKeyDown(Enter/F2) — keyboard rename for feeds | `SecondaryPages.jsx:822` | — |
| 2026-06-10 | v1.46.359 | UX | HomePage: keyboard access for BriefingRow, ArticleRow, featured article cards (tabIndex+onKeyDown+aria-label) | `HomePage.jsx:291,332,354,447,473` | — |
| 2026-06-10 | v1.46.358 | UX | ReadLaterPage Card+ListRow: role=button, tabIndex, onKeyDown, aria-label — keyboard accessibility for saved articles | `ReadLaterPage.jsx:305,414` | — |
| 2026-06-10 | v1.46.357 | UX | AnalyticsPage: role=tablist+tab+aria-selected on tab bar; aria-pressed on provider cards | `AnalyticsPage.jsx:478,248` | — |
| 2026-06-10 | v1.46.356 | UX | HighlightsDrawer export buttons: aria-label; ContentViewer Share aria-label reflects feedback | `HighlightsDrawer.jsx:53`, `ContentViewer.jsx:538` | — |
| 2026-06-10 | v1.46.355 | UX | Sidebar footer: Settings aria-label+aria-current; theme toggles aria-pressed; shortcuts aria-expanded | `Sidebar.jsx:632,655,660` | — |
| 2026-06-10 | v1.46.354 | UX | MobileFeedDrawer: role=dialog + aria-modal on bottom sheet — mobile nav drawer screen reader semantics | `MobileFeedDrawer.jsx:178` | — |
| 2026-06-10 | v1.46.353 | UX | OPMLImport modal: role=dialog + aria-modal + aria-label — screen reader modal semantics | `OPMLImport.jsx:127` | — |
| 2026-06-10 | v1.46.352 | UX | CardsPage "New card" modal: role=dialog + aria-modal — screen reader modal semantics | `CardsPage.jsx:379` | — |
| 2026-06-10 | v1.46.351 | UX | InboxPage: display sheet role=dialog+aria-modal; toast+OPML progress role=status+aria-live | `InboxPage.jsx:1322,1423,1513` | — |
| 2026-06-10 | v1.46.350 | UX | NotePanel + ArticleNotesPanel: role=dialog + aria-modal on bottom-sheets — screen reader modal semantics | `NotePanel.jsx:40`, `ArticleNotesPanel.jsx:47` | — |
| 2026-06-10 | v1.46.349 | UX | SelectionToolbar: role=toolbar + aria-label on highlight color buttons — screen reader accessible | `SelectionToolbar.jsx:102` | — |
| 2026-06-10 | v1.46.348 | Polish | OPMLImport checkbox: checkmark color="#fff" → T.accentText — readable in all themes | `OPMLImport.jsx:296` | — |
| 2026-06-10 | v1.46.347 | UX | NotesPage NoteCard: note-display div → role=button+keyboard — keyboard users can edit notes | `NotesPage.jsx:231` | — |
| 2026-06-10 | v1.46.346 | Polish | BottomNav CountBadge: color="#fff" → T.accentText — readable in all themes | `BottomNav.jsx:62` | — |
| 2026-06-10 | v1.46.345 | UX | PodcastPlayer VolumeSlider: role=slider + aria-valuemin/max/now + keyboard arrow keys | `PodcastPlayer.jsx:207` | — |
| 2026-06-10 | v1.46.344 | UX | InboxPage folder picker: div+onClick → button+role=menuitem, container role=menu — keyboard accessible | `InboxPage.jsx:1769` | — |
| 2026-06-10 | v1.46.343 | UX | Sidebar FeedContextMenu: role=menu + role=menuitem + aria-label — screen readers identify feed action menu | `Sidebar.jsx:129` | — |
| 2026-06-10 | v1.46.342 | UX | SearchBar: results → button + role=listbox + role=search — full keyboard-accessible search flow | `SearchBar.jsx:80,128,153` | — |
| 2026-06-10 | v1.46.341 | UX | PodcastPlayer: mini-player title area → button with aria-expanded — keyboard accessible expand | `PodcastPlayer.jsx:630` | — |
| 2026-06-10 | v1.46.340 | UX | NotesPage: NoteCard→role=button+keyboard, NoteListRow→button, article link→button — full keyboard nav | `NotesPage.jsx:72,144,121` | — |
| 2026-06-10 | v1.46.339 | UX | TagsInput: suggestion items → button with role=option in listbox — suggestions keyboard-selectable | `TagsInput.jsx:81` | — |
| 2026-06-10 | v1.46.338 | UX | Onboarding: feed selection cards → button + aria-pressed — keyboard accessible first-run flow | `Onboarding.jsx:91` | — |
| 2026-06-10 | v1.46.337 | UX | SecondaryPages Toggle: role=switch + aria-checked + keyboard support — screen readers announce switches correctly | `SecondaryPages.jsx:785` | — |
| 2026-06-10 | v1.46.336 | UX | HighlightsDrawer: highlight rows get role=button + tabIndex + onKeyDown — keyboard accessible | `HighlightsDrawer.jsx:74` | — |
| 2026-06-10 | v1.46.335 | UX | TodayPage: Feed Pulse + Reading Log rows converted from div+onClick to button — keyboard accessible | `TodayPage.jsx:208,268` | — |
| 2026-06-10 | v1.46.334 | Polish | LoginPage: replace isDark?#0e1117:#fff with T.accentText on logo + submit buttons — correct contrast all 6 themes | `LoginPage.jsx:89,134,165` | — |
| 2026-06-10 | v1.46.333 | Polish | SecondaryPages: "New Collection" hover uses T.accentText not #fff — fixes contrast on teal/blue/amber themes | `SecondaryPages.jsx:1318` | — |
| 2026-06-10 | v1.46.332 | Nav | MobileFeedDrawer: replace redundant Today/Saved pills with History/Stats — surfaces pages with no bottom-nav entry | `MobileFeedDrawer.jsx:284` | — |
| 2026-06-10 | v1.46.331 | Perf | MobileFeedDrawer: drag handle gets touch-action:pan-x for smooth compositor-threaded dismiss gesture | `MobileFeedDrawer.jsx:199` | — |
| 2026-06-10 | v1.46.330 | Perf | FeedItem SwipeRow: touch-action:pan-y removes iOS swipe jank by handing scroll to browser compositor | `FeedItem.jsx:163` | — |
| 2026-06-10 | v1.46.329 | Polish | SecondaryPages: folder move/color/delete/collapse buttons get aria-label + aria-expanded | `SecondaryPages.jsx:1012,1075,1080,1089,1114` | — |
| 2026-06-10 | v1.46.328 | Polish | ContentViewer: font-size range input gets aria-label + aria-valuetext | `ContentViewer.jsx:491` | — |
| 2026-06-10 | v1.46.327 | Polish | ContentViewer: reader prefs width/font buttons get aria-label + aria-pressed | `ContentViewer.jsx:500,508` | — |
| 2026-06-10 | v1.46.325 | Perf | Sidebar: feedsByFolder Map replaces per-folder feeds.filter() — O(feeds) not O(folders×feeds) | `Sidebar.jsx:422` | — |
| 2026-06-10 | v1.46.324 | Polish | HighlightsDrawer: role="dialog" + aria-modal + aria-labelledby + Close aria-label | `HighlightsDrawer.jsx:41` | — |
| 2026-06-09 | v1.46.323 | Polish | AddModal: role="dialog" + aria-labelledby; MobileSearchOverlay: role="search" on search bar | `AddModal.jsx:184` `MobileSearchOverlay.jsx:127` | — |
| 2026-06-09 | v1.46.322 | Polish | FolderModal + SmartFeedModal: role="dialog" + aria-modal + aria-labelledby + Close aria-labels | `FolderModal.jsx:43` `SmartFeedModal.jsx:52` | — |
| 2026-06-09 | v1.46.321 | Polish | ReviewPage: role="progressbar" + aria-valuenow for screen reader progress announcements | `ReviewPage.jsx:335` | — |
| 2026-06-09 | v1.46.320 | Polish | FeedItem: star color via T.amber.text token instead of hardcoded #F59E0B | `FeedItem.jsx:456,590` | — |
| 2026-06-09 | v1.46.319 | Perf | useBreakpoint: track category string not raw width — eliminates resize re-renders in 10+ components | `useBreakpoint.js:1` | — |
| 2026-06-09 | v1.46.318 | Polish | App: <main> landmark + aria-label on Sidebar and BottomNav <nav> elements | `App.jsx:301,331` `Sidebar.jsx:475` `BottomNav.jsx:133` | — |
| 2026-06-09 | v1.46.317 | Perf | MobileFeedDrawer: memo(FeedRow) with smart comparator — prevents re-renders during navigation | `MobileFeedDrawer.jsx:15` | — |
| 2026-06-09 | v1.46.316 | Polish | ReadLaterPage + InboxPage: aria-labels on save-URL toggle and mark-above-read button | `ReadLaterPage.jsx:130` `InboxPage.jsx:866` | — |
| 2026-06-09 | v1.46.315 | Polish | CardsPage: aria-label on export, sort toggle (aria-pressed), delete card buttons | `CardsPage.jsx:315,538,697` | — |
| 2026-06-09 | v1.46.314 | Perf | MobileFeedDrawer: memoize uncategorized feeds and totalUnread (prevent recompute during drag gestures) | `MobileFeedDrawer.jsx:157-158` | — |
| 2026-06-09 | v1.46.313 | Polish | Sidebar: aria-label+aria-pressed on theme toggles; aria-label on smart feed edit button | `Sidebar.jsx:359,614` | — |
| 2026-06-09 | v1.46.312 | Perf | ManageFeedsPage: memoize feed stats and filtered lists (stops repeated localStorage reads per render) | `SecondaryPages.jsx:1181-1194` | — |
| 2026-06-09 | v1.46.311 | Polish | ArticleNotesPanel: T.danger token for delete hover; aria-label on delete button | `ArticleNotesPanel.jsx:190` | — |
| 2026-06-09 | v1.46.310 | Polish | NotesPage: T.danger token for delete hover; bionic toggle role=switch/aria-checked; NoteEditor aria-label | `NotesPage.jsx:251,393` `ContentViewer.jsx:514` | — |
| 2026-06-09 | v1.46.309 | Polish | InboxPage: aria-label on mark-all-read, search, view-options buttons | `InboxPage.jsx:985,1010,1046` | — |
| 2026-06-09 | v1.46.308 | Polish | ContentViewer: aria-label on 5 icon-only buttons (prev/next, expand, reading prefs, open original, back to top) | `ContentViewer.jsx:419,443,452,474,549,759` | — |
| 2026-06-09 | v1.46.307 | Perf | HomePage: memoize dateLabel, topSources, totalUnread, folderUnreadMap | `HomePage.jsx:82-107` | — |
| 2026-06-09 | v1.46.306 | UX | Remove window.confirm from mark-all-read — reversible action, toast is sufficient feedback | `InboxPage.jsx:603` | — |
| 2026-06-09 | v1.46.305 | Polish | ContentViewer article date: relative format for recent (<7d) — consistent with FeedItem list display | `ContentViewer.jsx:23,709` | — |
| 2026-06-09 | v1.46.304 | UX | ContentViewer close button: aria-label="Close article" — screen reader + keyboard nav accessibility | `ContentViewer.jsx:397` | — |
| 2026-06-09 | v1.46.303 | Fix | Toast timer cleared on rapid successive calls — no early dismiss when save+bookmark fire together | `InboxPage.jsx:671` | — |
| 2026-06-09 | v1.46.302 | Polish | NotesPage article-link row: rgba(0,0,0,.06) → T.surface — invisible background on dark themes | `NotesPage.jsx:122` | — |
| 2026-06-09 | v1.46.301 | Polish | Settings theme selector border: rgba(0,0,0,0.12) → T.border — invisible on dark themes before | `SecondaryPages.jsx:566` | — |
| 2026-06-09 | v1.46.300 | Polish | CardsPage delete button: hardcoded #fee2e2/#ef4444 → T.danger tokens — correct across all 6 themes | `CardsPage.jsx:697` | — |
| 2026-06-09 | v1.46.299 | Perf | Lazy-load Readability inside fetchArticleContent — fetchers chunk 55KB→22KB; 34KB saved on initial inbox load | `fetchers.js:5,434` | — |
| 2026-06-09 | v1.46.298 | Nav | Reordered mobile bottom nav: Inbox→Today→Saved→Review→Cards — matches desktop sidebar order | `BottomNav.jsx:45` | — |
| 2026-06-09 | v1.46.297 | Perf | Memoized filtered list and time-bucket groups in ReadLaterPage — no re-filtering on every render | `ReadLaterPage.jsx:92` | — |
| 2026-06-09 | v1.46.296 | Perf | Memoized feedPulse, dateLabel, oldestSaved in TodayPage — feed list sort+map skips on re-renders | `TodayPage.jsx:110` | — |
| 2026-06-09 | v1.46.295 | Perf | Memoized HighlightedText token/segment computation — bionic split and highlight interval matching skip on re-renders | `ContentViewer.jsx:140` | — |
| 2026-06-09 | v1.46.294 | Perf | Memoized injectHtmlHighlights — no full-article regex on every scroll; only reruns when HTML/highlights change | `ContentViewer.jsx:85` | — |
| 2026-06-09 | v1.46.293 | Perf | Memoized AI summary bullet parsing in SummaryBlock — skips regex chain on re-renders; only recomputes on summary change | `ContentViewer.jsx:901` | — |
| 2026-06-09 | v1.46.292 | Perf | Memoized reading-time calc + favicon URL in ContentViewer — no article text split on scroll/highlight re-renders | `ContentViewer.jsx:73` | — |
| 2026-06-09 | v1.46.291 | Perf | Memoized uncategorized/PRIMARY_NAV/LIBRARY_NAV in Sidebar — no recreation on shortcuts/ctx-menu interactions | `Sidebar.jsx:422` | — |
| 2026-06-09 | v1.46.290 | Perf | dateBucket accepts pre-computed todayTs — eliminates 2 Date constructions per list item | `InboxPage.jsx:21,1269` | — |
| 2026-06-09 | v1.46.289 | Perf | Memoized unreadCount in InboxPage — skips O(n) filter on cursor/modal re-renders | `InboxPage.jsx:720` | — |
| 2026-06-09 | v1.46.288 | Perf | Memoized FeedRow in Sidebar with smart comparator — skips re-renders on shortcuts/context-menu interactions | `Sidebar.jsx:178` | — |
| 2026-06-09 | v1.46.287 | Perf | Memoized parseYouTubeUrl in ContentViewer — avoids URL parse on every scroll/highlight/TTS re-render | `ContentViewer.jsx:72` | — |
| 2026-06-09 | v1.46.286 | Nav | Sidebar Review badge shows due count — matches BottomNav parity; desktop users see pending reviews | `Sidebar.jsx:421`, `App.jsx:281` | — |
| 2026-06-09 | v1.46.285 | Perf | Memoized filteredItems + feedColorMap with useMemo — skips expensive array ops on UI-only re-renders | `InboxPage.jsx:290` | — |
| 2026-06-09 | v1.46.284 | Perf | Removed unused `lucide-react` dep — never imported, 2.1MB node_modules savings | `package.json`, `package-lock.json` | — |
| 2026-06-09 | v1.46.283 | Perf | Removed dead CSS — 5 unused @keyframes + 3 unused CSS classes from index.css | `index.css` | — |
| 2026-06-09 | v1.46.282 | Perf | Fixed 14 missed `transition:"all"` (no-space) — 0 remaining app-wide in any format | `Sidebar.jsx`, `CardsPage.jsx`, `LoginPage.jsx`, `InboxPage.jsx`, `SecondaryPages.jsx` | — |
| 2026-06-09 | v1.46.281 | Perf | Final 11 `transition: all` → targeted; zero remaining app-wide | `Onboarding.jsx`, `FolderModal.jsx`, `SmartFeedModal.jsx`, `HighlightsDrawer.jsx`, `OPMLImport.jsx`, `ArticleNotesPanel.jsx`, `NotesPage.jsx` | — |
| 2026-06-09 | v1.46.280 | Perf | Moved inline @keyframes to index.css; reused global spin in LoginPage | `ReviewPage.jsx:332`, `MobileSearchOverlay.jsx:126`, `LoginPage.jsx:81`, `index.css` | — |
| 2026-06-09 | v1.46.279 | Perf | FeedItem card icon + AnalyticsPage: last 3 `transition: all` in primary-screen components | `FeedItem.jsx:395`, `AnalyticsPage.jsx:252,489` | — |
| 2026-06-09 | v1.46.278 | Perf | SecondaryPages: 7× `transition: all` → targeted property lists | `SecondaryPages.jsx:562,1206,1218,1266,1273,1314,1502` | — |
| 2026-06-09 | v1.46.277 | Perf | CardsPage: 4× `transition: all` → targeted property lists | `CardsPage.jsx:446,468,542,700` | — |
| 2026-06-09 | v1.46.276 | Perf | InboxPage toolbar: 8× `transition: all` → targeted property lists | `InboxPage.jsx:932,978,991,1016,1033,1053,1083,1097` | — |
| 2026-06-09 | v1.46.263 | Perf | Session-level article content cache — re-opens instant, no proxy re-fetch; LRU cap 50 entries | `fetchers.js:20,395,458,562` | — |
| 2026-06-09 | v1.46.264 | Perf | preconnect hints for all 4 RSS proxy services in index.html — first feed fetch ~100–300ms faster | `index.html:30-35` | — |
| 2026-06-09 | v1.46.265 | UX | BottomNav dot badge → count badge (99+ cap) on Inbox and Review — unread count visible without opening page | `BottomNav.jsx:54-73,161-166` | — |
| 2026-06-09 | v1.46.266 | Fix | Progress bar on list items updates immediately on article close — was using stale module-level cache | `FeedItem.jsx:48-50`, `InboxPage.jsx:1437,1489` | — |
| 2026-06-09 | v1.46.267 | Nav | BottomNav auto-hides on scroll in Today + Saved pages — was missing navDirScroll handler | `TodayPage.jsx:10-20,135`, `ReadLaterPage.jsx:11-21,191` | — |
| 2026-06-09 | v1.46.268 | Perf | Moved fb-article-body CSS to index.css — ContentViewer chunk -2.7 kB, no per-open style injection | `ContentViewer.jsx:711-739`, `index.css:194-215` | — |
| 2026-06-09 | v1.46.269 | Polish | Page transitions: fadeInScale adds scale(0.97→1) — native-feel "settle" motion on every page switch | `index.css:51-54`, `App.jsx:310` | — |
| 2026-06-09 | v1.46.270 | Fix | FeedItem.jsx missing useRef import — crashed any page rendering feed items | `FeedItem.jsx:0` | — |
| 2026-06-09 | v1.46.271 | Polish | Swipe hint colors: 20%-opacity wash + action-matched text; amber hint was invisible in light mode | `FeedItem.jsx:135-139` | — |
| 2026-06-09 | v1.46.272 | Nav | BottomNav re-appears on article close in Today, Saved (ReadLater), History — missing fb-nav-dir dispatch | `TodayPage.jsx:308`, `ReadLaterPage.jsx:259`, `SecondaryPages.jsx:64` | — |
| 2026-06-09 | v1.46.273 | Fix | slideInUp moved to index.css — was only defined inside MobileFeedDrawer inline style, missing for InboxPage display sheet | `index.css:60-63`, `MobileFeedDrawer.jsx:361-370` | — |
| 2026-06-09 | v1.46.274 | Nav | Escape restores cursor to closed article; Sidebar shortcut labels corrected (L=Read later, S=Save) | `InboxPage.jsx:445`, `Sidebar.jsx:33` | — |
| 2026-06-09 | v1.46.275 | Perf | ContentViewer: 11× transition:all → targeted bg/color/border-color — stops browser watching all CSS props on reader hover | `ContentViewer.jsx:397,421,436,452,502,513,527,745,956,1054` | — |
| 2026-06-01 | v1.46.229 | Perf | Cap unbounded DB queries: getSaved(200), getAllHighlights(500), getHighlightReviews(200 ordered by next_review) | `supabase.js:125,532,549` | — |
| 2026-06-01 | v1.46.230 | Perf | Lazy-load AddModal + OPMLImport in InboxPage; InboxPage chunk 21→16 kB gz | `InboxPage.jsx:10-19,1449,1481` | — |
| 2026-06-01 | v1.46.231 | Fix | Worker proxy response now validated with looksLikeBlockPage; bot-challenge pages no longer silently pass through | `fetchers.js:59` | — |
| 2026-06-01 | v1.46.232 | Fix/Perf | getReadingStats date filter (366d) replaces limit(500); lazy-load MobileSearchOverlay; delete dead getAllHighlightsWithNotes | `supabase.js:491,540`, `InboxPage.jsx:18,1456` | — |
| 2026-06-08 | v1.46.248 | Fix | Podcast expanded panel on mobile: bottom 144px (above mini-bar), full viewport width | `PodcastPlayer.jsx:554` | — |
| 2026-06-08 | v1.46.249 | Nav | Reading Log rows in Today page now open articles in ContentViewer on click | `TodayPage.jsx:1,65,249` | — |
| 2026-06-09 | v1.46.250 | UX | Mobile default image size 72→96px; slider max 96→120px for larger phones | `InboxPage.jsx:67,1338` | — |
| 2026-06-09 | v1.46.251 | Perf | decoding="async" on ListThumb + MobileThumb — off-thread image decoding reduces scroll jank | `FeedItem.jsx:143,184` | — |
| 2026-06-09 | v1.46.252 | Perf | willChange: transform on BottomNav pill — GPU compositing for scroll-hide/show animation | `BottomNav.jsx:82` | — |
| 2026-06-09 | v1.46.253 | UX | Haptic feedback on feed item action buttons via navigator.vibrate(8) | `FeedItem.jsx:6,82` | — |
| 2026-06-09 | v1.46.254 | Polish | Active tab dot indicator on BottomNav — 4px accent dot below active icon | `BottomNav.jsx:143` | — |
| 2026-06-09 | v1.46.255 | UX | Mobile toolbar icon buttons 36→44px — meets iOS 44pt minimum touch target | `InboxPage.jsx:1095,1101,1114` | — |
| 2026-06-09 | v1.46.256 | UX | prefers-reduced-motion media query — collapses all CSS animations for accessibility | `index.css:193` | — |
| 2026-06-09 | v1.46.257 | UX | Scroll position saved per feed view via sessionStorage — restores on back-navigation | `InboxPage.jsx:132,140,1158` | — |
| 2026-06-09 | v1.46.258 | Polish | BottomNav labels — 9px text below each icon, bold when active | `BottomNav.jsx:95,126,144` | — |
| 2026-06-09 | v1.46.259 | Polish | Read-progress bar added to MobileThumb — partial-read indicator matches desktop | `FeedItem.jsx:170` | — |
| 2026-06-09 | v1.46.260 | UX | Swipe-to-act on list rows: right = mark read, left = save for later | `FeedItem.jsx:99` | — |
| 2026-06-09 | v1.46.261 | Polish | MobileThumb skeleton shimmer while image loads — replaces blank solid color | `FeedItem.jsx:240` | — |
| 2026-06-09 | v1.46.262 | Perf | Articles show RSS content instantly; Readability static import; removed auto-retry | `ContentViewer.jsx:104`, `fetchers.js:1,425` | — |
| 2026-06-08 | v1.46.247 | Fix | Podcast mini-bar bottom raised to 80px on mobile so it clears BottomNav pill; width fills viewport on mobile | `PodcastPlayer.jsx:609` | — |
| 2026-06-08 | v1.46.246 | Perf | Removed duplicate shimmer/@keyframes shimmer + --sk-base/--sk-shine from index.css — 22 lines of dead CSS | `index.css:77` | — |
| 2026-06-08 | v1.46.245 | Perf | Removed dead @keyframes pulse (scale variant) from index.css — overridden by opacity-only definition below it | `index.css:37` | — |
| 2026-06-08 | v1.46.244 | Fix | Saved page: onUnsave callback no longer calls removeReadLater (double-DELETE) — ContentViewer already handles the DB delete | `ReadLaterPage.jsx:248` | — |
| 2026-06-08 | v1.46.243 | Polish | Feed Pulse rows in Today: hover background added for visual consistency with QueueRow | `TodayPage.jsx:190` | — |
| 2026-06-08 | v1.46.242 | Perf | Saved page article images: loading="lazy" decoding="async" on Card+ListRow | `ReadLaterPage.jsx:304,403` | — |
| 2026-06-08 | v1.46.241 | Polish | Saved page cards show 2-line description snippet below title when available | `ReadLaterPage.jsx:347` | — |
| 2026-06-08 | v1.46.240 | Nav | Feed Pulse bars in Today now navigate to the specific feed (was `"inbox"`); feedId kept in chart data | `TodayPage.jsx:106,192` | — |
| 2026-06-08 | v1.46.239 | Feature | Rebuilt Saved page — source filter pills, time-bucketed groups, 3-col card grid with large images (desktop), compact list (mobile) | `ReadLaterPage.jsx`, `App.jsx:26`, `SecondaryPages.jsx` | — |
| 2026-06-01 | v1.46.233 | Nav | Due-cards dot badge on Review BottomNav icon via onDueCount callback | `ReviewPage.jsx:90,164`, `App.jsx:60,240,317`, `BottomNav.jsx:58,150` | — |
| 2026-06-01 | v1.46.227 | UX/Feature | Review summary badges; Cards color stripes; mobile tap-to-select sentence; Catch up smart feed | `ReviewPage.jsx`, `CardsPage.jsx`, `SelectionToolbar.jsx`, `Sidebar.jsx`, `InboxPage.jsx`, `App.jsx` | — |
| 2026-05-31 | v1.46.221 | UX | Tags bar: remove borderBottom divider + "TAGS" uppercase label — less chrome, content feels closer | `ContentViewer.jsx:568,570` | — |
| 2026-05-31 | v1.46.222 | UX/Polish | Review page: remove divider, inline Add annotation, swipe hint, BookOpen empty state icon | `ReviewPage.jsx` | — |
| 2026-05-31 | v1.46.223 | Nav | BottomNav: remove RevealLabel entirely; inbox count badge → 6px dot | `BottomNav.jsx` | — |
| 2026-05-31 | v1.46.224 | UX | Mobile search: Spotlight overlay (blur backdrop, 18% from top) replaces toolbar inline; iOS zoom fix | `InboxPage.jsx`, `SearchBar.jsx` | — |
| 2026-05-31 | v1.46.225 | UX | Mobile search rebuilt as MobileSearchOverlay — full-screen, search bar pinned top, results scroll inline | `MobileSearchOverlay.jsx`, `InboxPage.jsx` | — |
| 2026-05-31 | v1.46.226 | Fix/Nav | Bookmark rebuild: onSave/onUnsave sync savedItems; saved filter refreshes on activate; Saved tab in BottomNav | `InboxPage.jsx`, `BottomNav.jsx`, `SecondaryPages.jsx` | — |
| 2026-05-26 | v1.46.201 | Feature/UX/Fix | Podcast search tab in AddModal (Apple Podcasts); mobile folders collapsed by default; Settings chip added to mobile drawer | `AddModal.jsx`, `MobileFeedDrawer.jsx` | — |
| 2026-05-24 | v1.46.199 | Fix/Polish/Perf | Highlight picker: HIGHLIGHT_COLORS fixes pink/purple mismatch; save hover uses T.danger; hero image decoding="async" | `ContentViewer.jsx:1285,500,646,1453` | — |
| 2026-05-23 | v1.46.198 | Feature | Cards first-class: TagsInput with autocomplete, color stripe, hover-delete, search, accent tiles; TagsInput accent pill borders + click-to-filter | `CardsPage.jsx`, `TagsInput.jsx` | — |
| 2026-05-23 | v1.46.197 | Fix | Saving broken: saveItem lacked is_read_later=true; reader save button now toggles; isSaved wired to list view; reader font 20px, wider column, better CSS | `supabase.js`, `ContentViewer.jsx`, `InboxPage.jsx`, `readerPrefs.js` | — |
| 2026-05-23 | v1.46.196 | Nav | Mobile: Settings button in top toolbar; toolbar height 72px; 40px tap targets; larger title + icons; filled unread badge | `InboxPage.jsx`, `App.jsx` | — |
| 2026-05-23 | v1.46.195 | UX | List items larger: mobile rows 14px pad/20px title/104px thumb; desktop vPad+fontSize up at all tiers; default cardSize→lg; BottomNav strokeWidth thinner | `FeedItem.jsx`, `BottomNav.jsx`, `InboxPage.jsx` | — |
| 2026-05-23 | v1.46.194 | Nav | Sidebar nav grouped into two clusters (Inbox/Today vs Saved/Review/Cards) with hairline divider; "Smart" and "Feeds" section labels added to feed tree | `Sidebar.jsx` | — |
| 2026-05-23 | v1.46.193 | Feature | Appearance section in Settings: 6-theme swatch picker; sidebar Light/Dark toggle simplified (active for theme family) | `SecondaryPages.jsx`, `Sidebar.jsx` | — |
| 2026-05-22 | v1.46.192 | Feature | 3 new themes: Cream, Ink, Sepia (editorial palette from design handoff); swatch picker; on-demand Cormorant loading | `tokens.js`, `useTheme.jsx`, `SecondaryPages.jsx`, `Sidebar.jsx` | — |
| 2026-05-22 | v1.46.191 | Fix | Auto-mark-read cascade: fixed fontWeight to 500 (no layout shift), debounce observer 200ms, rootMargin:-20px buffer | `FeedItem.jsx:318,377,510`, `InboxPage.jsx:590-610` | — |
| 2026-05-22 | v1.46.190 | Polish | Row separators, read-item fade 50%, toolbar hairline, card≠bg, BottomNav shadow theme-aware | `FeedItem.jsx`, `InboxPage.jsx`, `tokens.js`, `BottomNav.jsx` | — |
| 2026-05-22 | v1.46.189 | Perf | Drop all Google Fonts; switch to system font stack + ui-serif reader font; Light theme to crisp white/iOS-grey | 8 files | — |
| 2026-05-22 | v1.46.188 | Polish | List view: remove fadeInUp animation, default cardSize lg→md, tighten row padding — less visual bulk | `InboxPage.jsx:42,992,1007`, `FeedItem.jsx:288,345` | — |
| 2026-05-22 | v1.46.187 | Fix | New-articles banner count: track new URLs in ref, only decrement for new items, reset on source/folder/filter change | `InboxPage.jsx:55-56,163,275,453,938` | — |
| 2026-05-22 | v1.46.186 | Polish | Sidebar header: corrected "Feed Box" → "Feedbox" — consistent brand name | `Sidebar.jsx:352` | — |
| 2026-05-15 | v1.46.170 | Nav | Today page added to mobile BottomNav; fixed feeds isActive bug that lit up Feeds when on Today | `BottomNav.jsx:25-31` | — |
| 2026-05-15 | v1.46.171 | UX | Today page mobile: 2-col card grid → full-width list rows with 14px font and large tap targets | `TodayPage.jsx:467-544` | — |
| 2026-05-15 | v1.46.172 | Feature | StatPills interactive: "cards due" → Review, "saved" → Read Later; onNavigate wired to TodayPage | `TodayPage.jsx:363-408`, `App.jsx:217` | — |
| 2026-05-15 | v1.46.173 | Fix | Vitest restored: node env + removed jest-dom setup; 17 tests pass in 13s (was 60s timeout) | `vitest.config.js`, `src/test/setup.js` | — |
| 2026-05-19 | v1.46.180 | Polish | Today desktop MagazineTile: hero+medium overlay+text conditional on hasImage — fix Light theme | `TodayPage.jsx:457-512` | — |
| 2026-05-19 | v1.46.179 | Perf | BottomNav: drop lucide-react, replace List/Inbox with inline SVGs matching Sidebar style | `BottomNav.jsx:1-35` | — |
| 2026-05-16 | v1.46.178 | Polish | Today SnapCard: no-image cards use theme-native colors (T.text, T.border) — fix white text on light bg | `TodayPage.jsx:268-320` | — |
| 2026-05-15 | v1.46.177 | Feature | Today mobile: replace custom swipe engine with CSS scroll-snap; one card per swipe; remove Prev/Next strip | `TodayPage.jsx:232-430` | — |
| 2026-05-15 | v1.46.176 | Fix | Today mobile: two-phase slide transition, navigating lock, BottomNav clearance, flick thresholds | `TodayPage.jsx:232-370` | — |
| 2026-05-15 | v1.46.175 | Fix | Today mobile swipe: imperative ref transforms, passive:false touchmove, velocity flick detection | `TodayPage.jsx:232-330` | — |
| 2026-05-15 | v1.46.174 | Feature | Today page Flipboard redesign: mobile swipeable card deck + desktop magazine tile grid | `TodayPage.jsx` (full rewrite) | — |
| 2026-05-08 | v1.46.166 | Feature | New card button + modal on Cards page — create cards without highlighting in an article | `CardsPage.jsx` | — |
| 2026-05-08 | v1.46.164 | Nav | BottomNav scroll-hide extended to TodayPage and CardsPage — consistent chrome-recede on all primary screens | `TodayPage.jsx:134, CardsPage.jsx:83,198` | — |
| 2026-05-07 | v1.46.163 | UX | Reader top bar auto-hides on scroll-down, reappears on scroll-up — chrome recedes while reading | `ContentViewer.jsx:64,234,360` | — |
| 2026-05-07 | v1.46.162 | UX | Review rating buttons: add touch feedback (opacity+scale) — buttons feel responsive on mobile | `ReviewPage.jsx:269` | — |
| 2026-04-17 | v1.46.161 | Feature | Mobile image size toggle in list view: cycle hidden/small/large thumbnails | `InboxPage.jsx:777` | — |
| 2026-04-17 | v1.46.160 | Feature | Today page revamp: magazine grid layout, HeroCard, StatPills, ArticleGrid | `TodayPage.jsx` | — |
| 2026-04-17 | v1.46.159 | Nav | Sidebar folders default collapsed; expand state persisted to localStorage | `Sidebar.jsx:295` | — |
| 2026-04-17 | v1.46.158 | Feature | Podcast player revamp: mini-bar + full-screen mobile, draggable seek, volume, resume position | `PodcastPlayer.jsx` | — |
| 2026-04-17 | v1.46.157 | UX | Reader toolbar Save+Share → icon-only buttons; Save fills solid when saved | `ContentViewer.jsx:474` | — |
| 2026-04-17 | v1.46.156 | UX | Today source group headers: remove borderBottom + article count — chrome recedes | `TodayPage.jsx:259` | — |
| 2026-04-17 | v1.46.155 | Polish | Sidebar Analytics active bg T.surface → T.accentSurface — matches all other nav items | `Sidebar.jsx:487` | — |
| 2026-04-17 | v1.46.154 | UX | Reader toolbar: external-link icon button to open original article | `ContentViewer.jsx:488` | — |
| 2026-04-17 | v1.46.153 | Polish | BottomNav Inbox badge → dot — matches sidebar dots, consistent mobile/desktop | `BottomNav.jsx:140` | — |
| 2026-04-17 | v1.46.152 | UX | Reader title → clickable link to original article, opens in new tab | `ContentViewer.jsx:629` | — |
| 2026-04-17 | v1.46.151 | Polish | Sidebar unread counts → dot indicators — removes number anxiety, consistent with collapsed nav dots | `Sidebar.jsx:80,132,198` | — |
| 2026-04-17 | v1.46.150 | Feature | Today page: AI morning brief → 3-widget dashboard (Streak, Review Due, Saved) | `TodayPage.jsx` | — |
| 2026-04-12 | v1.46.149 | Polish | Remove duplicate borderLeft from ContentViewer — split-view divider was 2px instead of intended 1px; screen-edge border in full-screen mode eliminated | `ContentViewer.jsx:341` | — |
| 2026-04-12 | v1.46.148 | Polish | Card hover border → background elevation (T.surface2) — eliminates border-flash, calmer hover feedback | `FeedItem.jsx:448` | — |
| 2026-04-11 | v1.46.147 | UX | BottomNav auto-hides on scroll-down, reappears on scroll-up — content-first; consumes existing fb-nav-dir event | `BottomNav.jsx:25` | — |
| 2026-04-11 | v1.46.146 | Polish | Mobile source label fontWeight 500→400 — fixes hierarchy inversion on read items where source outweighed title | `FeedItem.jsx:301` | — |
| 2026-04-10 | v1.46.145 | Polish | Desktop list action button panel: T.bg → T.surface/accentSurface — eliminates visible backdrop mismatch on row hover | `FeedItem.jsx:411` | — |
| 2026-04-10 | v1.46.144 | Polish | Desktop list md row padding 13px → 11px — natural follow-up to description removal; ~1 more article above the fold | `FeedItem.jsx:345` | — |
| 2026-04-09 | v1.46.143 | Polish | Remove description excerpt from default (md) desktop list view — title is now dominant, faster headline scanning | `FeedItem.jsx:386` | — |
| 2026-04-09 | v1.46.142 | Nav | Collapsed sidebar active folder: T.surface → T.accentSurface — active was indistinguishable from hover | `Sidebar.jsx:160` | — |
| 2026-04-09 | v1.46.140 | UX | Reduce mobile feed row padding 14→11px — shows ~1 more item above the fold | `FeedItem.jsx:288` | — |
| 2026-04-09 | v1.46.139 | Polish | Mobile list title clamp 3 → 2 lines — row height ~144px → ~120px, one extra article visible above the fold | `FeedItem.jsx:321` | — |
| 2026-04-08 | v1.46.137 | Feature | Today AI morning brief — daily headline summary, cached by date, ~$0.0013/call | `TodayPage.jsx:28`, `summarize/index.ts:26` | — |
| 2026-04-08 | v1.46.136 | Polish | Skeleton card border removed — loading state now matches borderless FeedItem card appearance | `InboxPage.jsx:1192` | — |
| 2026-04-08 | v1.46.135 | Fix | Optimistic mark-read — sidebar counts update instantly, Supabase write moves to background | `InboxPage.jsx:438` | — |
| 2026-04-08 | v1.46.133 | Fix | getReadUrls scoped to 90 days — fixes 1000-row Supabase cap causing old read items to reappear as unread | `supabase.js:288` | — |
| 2026-04-08 | v1.46.132 | Polish | Swipe action buttons: replace 5 hardcoded iOS hex values with T.accent/T.success/T.warning/T.surface2/T.accentText | `FeedItem.jsx:144,148,157,162,167` | — |
| 2026-04-07 | v1.46.131 | Polish | Sidebar footer icon buttons: inactive border transparent — eliminates T.border noise; accent border kept for active state | `Sidebar.jsx:515,548,553` | — |
| 2026-04-06 | v1.46.129 | Fix | AI settings now work: summarize routes through Supabase Edge Fn (reads app_secrets/app_config); ai_usage tracking added | `fetchers.js:802`, `summarize/index.ts:81` | — |
| 2026-04-06 | v1.46.128 | Polish | Inbox toolbar title 14px → 17px — creates dominant heading, serves Typography-as-design hierarchy principle | `InboxPage.jsx:630` | — |
| 2026-04-06 | v1.46.127 | Fix | YouTube thumbnails + Slickdeals images: media:thumbnail before media:content, yt:videoId fallback, DOMParser extraction | `fetchers.js:281,158,180` | — |
| 2026-04-06 | v1.46.126 | UX | Remove borderBottom from reader top bar — blur+bg already separates header; border competed with article first line | `ContentViewer.jsx:362` | — |
| 2026-04-06 | v1.46.124 | Nav | Sidebar active item uses T.accentSurface — previously T.surface was identical to hover, active page indistinguishable | `Sidebar.jsx:62` | — |
| 2026-04-06 | v1.46.123 | Polish | Replace card-view hover drop shadow with T.border — removes decorative chrome, matches flat hover pattern in list view | `FeedItem.jsx:451` | — |
| 2026-04-05 | v1.46.122 | Polish | Remove decorative boxShadow from list-item hover action tray — no chrome, T.bg contrast sufficient | `FeedItem.jsx:411` | — |
| 2026-04-05 | v1.46.121 | Polish | Read titles on mobile use T.textTertiary (not textSecondary) — matches desktop, sharpens unread/read hierarchy | `FeedItem.jsx:318` | — |
| 2026-04-04 | v1.46.120 | Polish | Remove decorative border from unselected card items — gap+radius define shape | `FeedItem.jsx:450` | — |
| 2026-04-04 | v1.46.119 | Polish | Remove boxShadow from active Unread/All toggle pill — T.bg+weight already signal selection | `InboxPage.jsx:744` | — |
| 2026-04-04 | v1.46.118 | Polish | BottomNav pill bigger: padding 11px, icons 24px, Add 42px, labels 11px | `BottomNav.jsx:37,68,87,99,115,128,141` | — |
| 2026-04-04 | v1.46.117 | Nav | Feeds pill active on Today+Saved pages — pill always shows a selected item | `BottomNav.jsx:76` | — |
| 2026-04-04 | v1.46.116 | Polish | Page transitions: fadeIn → fadeInScale for spatial depth on every navigation | `App.jsx:257` | — |
| 2026-04-04 | v1.46.115 | UX | SwipeRow: add onTouchCancel to reset stuck swipe state on OS interruptions | `FeedItem.jsx:177` | — |
| 2026-04-04 | v1.46.114 | UX | Remove reading time footer from sm cards — eliminates dead space on mobile grid | `FeedItem.jsx:526-537` | — |
| 2026-04-04 | v1.46.113 | Polish | Active BottomNav tab gets accentSurface background pill — clear active state beyond color alone | `BottomNav.jsx:83-120` | — |
| 2026-04-04 | v1.46.112 | Nav | Restore Today+Saved on mobile via compact pill buttons in Feeds bottom sheet | `MobileFeedDrawer.jsx:232-252` | — |
| 2026-04-04 | v1.46.111 | Polish | Card items: 1px border + 14px radius + hover shadow — clear definition on all themes | `FeedItem.jsx:447-450` | — |
| 2026-04-04 | v1.46.110 | Fix | Auto-mark-read on scroll: observer recreated on every read due to readUrls dep; fixed with readUrlsRef | `InboxPage.jsx:44-46,579,584` | — |
| 2026-04-04 | v1.46.109 | Polish | AddModal polished: RSS icon header + divider, larger input with glow focus, accent badge for detected type | `AddModal.jsx:115-260` | — |
| 2026-04-04 | v1.46.108 | Polish | AddModal redesigned: X replaces Cancel, no header block, input as hero, slim type label | `AddModal.jsx:1-200` | — |
| 2026-04-04 | v1.46.106 | Nav | Remove Add slot from pill nav (4 items); strip redundant nav rows from Feeds bottom sheet | `BottomNav.jsx:12-18` `MobileFeedDrawer.jsx:111-273` | — |
| 2026-04-04 | v1.46.105 | Polish | Pill nav closer to bottom (16→8px) and larger tap targets (padding 10→12px) | `BottomNav.jsx:26,68,105` | — |
| 2026-04-03 | v1.46.104 | Nav | Pill nav always visible; mobile toolbar 48→62px; feed content fills to screen bottom | `BottomNav.jsx:21-32` `InboxPage.jsx:623,909` `App.jsx:257` | — |
| 2026-04-03 | v1.46.103 | Nav | BottomNav converted to floating pill — centered, lifted off bottom, pill border-radius + shadow | `BottomNav.jsx:34-54` | — |
| 2026-04-03 | v1.46.102 | Nav | BottomNav rewritten with lucide-react icons — declarative NAV_ITEMS array, strokeWidth for active state | `BottomNav.jsx:1-157` | — |
| 2026-04-03 | v1.46.96 | Polish | Remove decorative border from inactive source filter pill — reduces toolbar noise | `InboxPage.jsx:691` | — |
|------|---------|------|--------|-------|-------------|
| 2026-04-03 | v1.46.101 | Nav | BottomNav reappears after closing mobile reader — dispatch fb-nav-dir "up" on ContentViewer onClose | `InboxPage.jsx:1037` | — |
| 2026-04-03 | v1.47.00 | Polish | Review page highlight strips now use HIGHLIGHT_COLORS from SelectionToolbar — matches Cards page colors | `ReviewPage.jsx:6,45,159` | — |
| 2026-04-03 | v1.46.99 | Perf | Lazy-load fuse.js on first search keystroke — defers 6.6 kB gz from InboxPage initial fetch | `SearchBar.jsx:2,46` | — |
| 2026-04-03 | v1.46.98 | Polish | SearchBar match highlight: hardcoded teal → T.accent at 33% opacity, matches all three themes | `SearchBar.jsx:211` | — |
| 2026-04-03 | v1.46.96 | Polish | Today hero card: remove 65% opacity on read state; title dims to T.textTertiary, image stays crisp | `TodayPage.jsx:397,441` | — |
| 2026-04-03 | v1.46.95 | Polish | Today page read items: whole-row opacity→title-only T.textTertiary; images keep full presence | `TodayPage.jsx:519,529,541` | — |
| 2026-04-03 | v1.46.94 | Polish | Card view source labels T.accent→T.textSecondary, weight 600→500 — matches list view v1.46.90 fix | `FeedItem.jsx:494` | — |
| 2026-04-03 | v1.46.93 | Fix | Card view podcast items no longer show misleading "X min read" alongside audio duration | `FeedItem.jsx:526,531` | — |
| 2026-04-03 | v1.46.92 | Polish | Remove redundant borderTop hairline from BottomNav — backdrop-filter glass provides separation | `BottomNav.jsx:59` | — |
| 2026-04-02 | v1.46.91 | Feature | Podcast panel player — episodes open in right panel with inline audio controls, no modal | `ContentViewer.jsx:1273` `InboxPage.jsx:963,978` | — |
| 2026-04-02 | v1.46.90 | Polish | Mute mobile source label T.accent→T.textSecondary, weight 600→500 for calmer hierarchy | `FeedItem.jsx:300` | — |
| 2026-04-01 | v1.46.89 | Polish | Remove redundant borderTop from CardItem hover action bar | `FeedItem.jsx:541` | — |
| 2026-04-01 | v1.46.88 | Polish | Increase mobile list item padding 11px→14px for more generous whitespace between Inbox rows | `FeedItem.jsx:287` | — |
| 2026-03-31 | v1.46.87 | Polish | Replace hardcoded rgba sage-green in reading-progress bars with T.accent token | `FeedItem.jsx:242,483` | — |
| 2026-03-31 | v1.46.86 | Fix | RSS content fallback + Cloudflare block detection + SlickDeals/AlternativeTo selectors | `fetchers.js:351,430` `ContentViewer.jsx:100,630` | — |
| 2026-03-31 | v1.46.85 | Feature | Spotify/Apple Podcast link support — oEmbed→iTunes search resolution, "Spotify Podcast" chip | `fetchers.js:565,660` `AddModal.jsx:4,8` | — |
| 2026-03-31 | v1.46.84 | Feature | Redesign PodcastPlayer — centered modal on desktop, full-screen sheet on mobile with blurred art bg | `PodcastPlayer.jsx:1-280` | — |
| 2026-03-31 | v1.46.83 | Polish | Standardise BottomNav active label fontWeight 700→600 — consistent with Feeds tab, less jarring jump | `BottomNav.jsx:171` | — |
| 2026-03-31 | v1.46.82 | Polish | Add OG/Twitter Card meta tags — improves share previews and SEO score | `index.html:22` | — |
| 2026-03-31 | v1.46.81 | Polish | Remove decorative boxShadow divider from Inbox toolbar — no decorative chrome principle | `InboxPage.jsx:623` | — |
| 2026-03-30 | v1.46.80 | Feature | Persist AI summaries in localStorage — summaries survive reader close/reopen | `ContentViewer.jsx:35,65,258` | — |
| 2026-03-30 | v1.46.68 | Polish | Remove borderBottom divider from mobile list-view items — whitespace separates rows | `FeedItem.jsx:290` | — |
| 2026-03-27 | v1.46.13 | Nav | Renamed sidebar label "All Items" → "Inbox" to match mobile nav | `Sidebar.jsx:329` | — |
| 2026-03-27 | v1.46.14 | Nav | Added `onTouchCancel` to all 3 BottomNav button types — fixes buttons stuck dimmed after scroll gesture | `BottomNav.jsx:66,103,131` | — |
| 2026-03-27 | v1.46.15 | Polish | Fixed hardcoded `color:"#fff"` on Add button and unread badge — use `T.accentText` token for correct contrast on all themes | `BottomNav.jsx:72,139` | — |
| 2026-03-27 | v1.46.16 | Nav | Fixed "All Items" label in MobileFeedDrawer — now reads "Inbox" matching Sidebar and BottomNav | `MobileFeedDrawer.jsx:258` | — |
| 2026-03-27 | v1.46.17 | Polish | Replaced clock icon with bookmark for "Saved" in Sidebar — matches BottomNav icon and label | `Sidebar.jsx:12` | — |
| 2026-03-27 | v1.46.18 | Nav | Added "Home" as first Sidebar nav item — dashboard had no return path after navigating away | `Sidebar.jsx:13,330` | — |
| 2026-03-27 | v1.46.19 | Nav | Added "History" to Sidebar nav — page existed in router but was unreachable from any nav surface | `Sidebar.jsx:14,334` | — |
| 2026-03-27 | v1.46.20 | Polish | Fixed `color:"#fff"` on MobileFeedDrawer Add Source button — use `T.accentText` for correct Nocturne contrast | `MobileFeedDrawer.jsx:246` | — |
| 2026-03-27 | v1.46.21 | Polish | Fixed hardcoded Nocturne #accfae in NotesPage TagCard — tag pills and divider now use T.accent tokens across all themes | `NotesPage.jsx:284-290` | — |
| 2026-03-27 | v1.46.22 | Polish | Replaced ⚙ emoji with SVG Analytics icon on Sidebar admin button — crisp retina rendering, consistent with icon system | `Sidebar.jsx:14,536` | — |
| 2026-03-27 | v1.46.23 | Polish | Fixed hardcoded `#03210b` in NotesPage and ArticleNotesPanel — use T.accentText for correct contrast on all themes | `NotesPage.jsx:675, ArticleNotesPanel.jsx:146` | — |
| 2026-03-27 | v1.46.24 | Polish | Fixed hardcoded `#e53e3e` on feed error badge in InboxPage — now uses T.danger token | `InboxPage.jsx:641` | — |
| 2026-03-27 | v1.46.25 | UX | Added aria-label + aria-current to all BottomNav buttons — Add button was invisible to screen readers | `BottomNav.jsx:55,92,120` | — |
| 2026-03-27 | v1.46.26 | UX | Added aria-label + aria-current to Sidebar NavItem and collapse/expand toggle buttons | `Sidebar.jsx:51-52,368,375` | — |
| 2026-03-27 | v1.46.27 | UX | Moved Admin button directly below Sources in Sidebar — groups management tools together | `Sidebar.jsx:484` | — |
| 2026-03-27 | v1.46.28 | UX | Improved TodayPage empty state — differentiates no-feeds (setup prompt) from quiet-feeds (reassurance) | `TodayPage.jsx:166` | — |
| 2026-03-27 | v1.46.29 | Polish | Fixed hardcoded `color:"#fff"` on 5 accent buttons in SecondaryPages — Add feed, PRO badge, Enable notifications, Export OPML, Export All now use T.accentText | `SecondaryPages.jsx:119,450,614,827,1688` | — |
| 2026-03-27 | v1.46.30 | Polish | Fixed hardcoded `color:"#fff"` on 3 accent buttons in InboxPage — New articles banner, Add Feed CTA, and Retry button now use T.accentText | `InboxPage.jsx:903,1090,1307` | — |
| 2026-03-27 | v1.46.31 | Feature | AI-suggested tags on article open — Haiku suggests 3–5 tags as one-tap pills in the tags bar for Pro users | `fetchers.js:832, ContentViewer.jsx:50,113,468` | — |
| 2026-03-27 | v1.46.32 | Feature | Spaced repetition Daily Review — SM-2 scheduling in localStorage, Review page with Again/Good/Easy buttons, added to Sidebar nav | `ReviewPage.jsx:1-175, App.jsx:21,222, Sidebar.jsx:17,341` | — |
| 2026-03-28 | v1.46.33 | Polish | Fixed last 5 hardcoded `color:"#fff"` on T.accent elements — AnalyticsPage ACTIVE badge + Save button, TodayPage read badge, NotesPage Save button, Onboarding checkmark | `AnalyticsPage.jsx:259,316, TodayPage.jsx:419, NotesPage.jsx:221, Onboarding.jsx:109` | — |
| 2026-03-28 | v1.46.34 | Nav+Feature | Removed Home/History from Sidebar; revamped Today — single-line stat header, source-grouped article sections | `Sidebar.jsx:334, TodayPage.jsx:94-230,265-330` | — |
| 2026-03-28 | v1.46.35 | UX | Added "Start Reading"/"Continue Reading" CTA to Today header — opens first unread article and guides user through the queue | `TodayPage.jsx:113,130,293,320` | — |
| 2026-03-28 | v1.46.36 | Polish | Added `dangerText` token to all 3 themes — Distilled danger (#ffb4ab) made white text unreadable; UI.jsx, InboxPage, SecondaryPages now use T.dangerText | `tokens.js:51,76, UI.jsx:32, InboxPage.jsx:641, SecondaryPages.jsx:1706` | — |
| 2026-03-29 | v1.46.37 | Polish | Fixed 4 hardcoded `color:"#fff"` on T.accent elements in SecondaryPages — PRO badges (×2), Export button, Save button now use T.accentText | `SecondaryPages.jsx:301,485,690,1758` | — |
| 2026-03-29 | v1.46.38 | Feature | Notecard theme tagging on highlights — "+ theme" pills in HighlightsDrawer, tags[] column in DB schema, updateHighlightTags helper | `HighlightsDrawer.jsx:1-20,70-95, ContentViewer.jsx:12,152, supabase.js:167, supabase-schema.sql:48` | — |
| 2026-03-29 | v1.46.39 | Feature | Cards page — theme-first notecard browser; bucket grid + drill-in card list; Cards icon + nav item in Sidebar | `CardsPage.jsx:1-145, App.jsx:23,225, Sidebar.jsx:18,340` | — |
| 2026-03-29 | v1.46.40 | Polish | Fixed 8 hardcoded `color:"#fff"` on T.accent elements across modals/components — PlanGate, FolderModal, SmartFeedModal, AddModal, DigestModal, PWAInstallBanner, Onboarding | `PlanGate.jsx:53,63, FolderModal.jsx:105, SmartFeedModal.jsx:114, AddModal.jsx:158, DigestModal.jsx:104, PWAInstallBanner.jsx:99, Onboarding.jsx:126` | — |
| 2026-03-29 | v1.46.41 | UX | Added Review and Cards to MobileFeedDrawer — both pages were unreachable on mobile; completes notecard system mobile parity | `MobileFeedDrawer.jsx:270-275` | — |
| 2026-03-29 | v1.46.42 | Polish | Fixed hardcoded `#22C55E` on Today progress bar — 100% state now uses T.success for correct color on all themes | `TodayPage.jsx:360` | — |
| 2026-03-29 | v1.46.43 | Perf | Lazy-load ContentViewer in InboxPage — defers 14 kB gz reader chunk until first article open instead of loading with InboxPage | `InboxPage.jsx:10,988-1024` | — |
| 2026-03-30 | v1.46.44 | Perf | Non-blocking Google Fonts — preload+onload swap pattern eliminates ~773ms render-blocking penalty on initial paint | `index.html:10` | — |
| 2026-03-30 | v1.46.45 | Feature | "Ask about this article" Q&A in AI Summary card — input + answer display below format switcher, calls askQuestion() in fetchers.js | `fetchers.js:832, ContentViewer.jsx:625,845,884` | — |
| 2026-03-30 | v1.46.46 | Security | Removed all client-side AI API keys — all AI calls now route through Cloudflare Worker only; deleted localStorage key storage, VITE env paths, and direct browser API calls | `fetchers.js:700-894, apiKeys.js, SecondaryPages.jsx:742-764, cloudflare-worker/worker.js` | — |
| 2026-03-30 | v1.46.47 | Feature | Removed Notes — annotation pivots to highlight-first; NotesPage + ArticleNotesPanel removed; nav updated across Sidebar/BottomNav/MobileFeedDrawer; legacy notes route → Cards | `App.jsx:20,223, Sidebar.jsx:339, MobileFeedDrawer.jsx:267, ContentViewer.jsx:17,45-47,425-437,692-701` | — |
| 2026-03-30 | v1.46.48 | Feature | Highlight creates a card in one action — NotePanel opens immediately after highlight; NotePanel gains inline theme-tag input (passage + note + tag in one step) | `ContentViewer.jsx:138, NotePanel.jsx:1-95` | — |
| 2026-03-30 | v1.46.49 | Nav | Replaced "Saved" with "Cards" in BottomNav — completes the mobile reading loop (Inbox → highlight → Cards) | `BottomNav.jsx:15,23,31` | — |
| 2026-03-30 | v1.46.51 | Fix | Highlight toolbar position — changed to `position: fixed` so it always appears above selected text regardless of reader scroll position | `SelectionToolbar.jsx:31,62` | — |
| 2026-03-30 | v1.46.52 | Feature | Readwise-style review — fixed passage field bug, 5-card daily limit, swipe gestures, tags + annotation on card, natural button labels | `ReviewPage.jsx:1-230` | — |
| 2026-03-30 | v1.46.53 | Polish | Fixed 7 hardcoded colors from /polish audit — FolderModal, Sidebar, DigestModal, SecondaryPages, AnalyticsPage now use T.danger/T.accentText | `FolderModal.jsx:97, Sidebar.jsx:488,532, DigestModal.jsx:71, SecondaryPages.jsx:599,1296,1404, AnalyticsPage.jsx:316` | — |
| 2026-03-30 | v1.46.54 | Feature | Added "Untagged" bucket to CardsPage — highlights without theme tags were invisible; now browsable in a dedicated bucket | `CardsPage.jsx:38,50,61,115-160` | — |
| 2026-03-30 | v1.46.55 | Polish | Fixed ErrorBoundary crash screen hardcoded to Nocturne — reads localStorage theme at render time; Light + Distilled users now see correct colors | `UI.jsx:113-138` | — |
| 2026-03-30 | v1.46.56 | Nav | Removed "Add Source" pill from sidebar bottom; moved theme toggles inline with user/shortcuts row — cleaner footer, + icon in Feeds header is sufficient | `Sidebar.jsx:518-578` | — |
| 2026-03-30 | v1.46.57 | Nav | Floating FAB (Things 3-style), smart feeds above feed list, starred articles show yellow filled star, CLAUDE.md token efficiency mandate | `App.jsx:266, Sidebar.jsx:399-466, FeedItem.jsx:52-70,416,545, InboxPage.jsx:6,44,128,476` | — |
| 2026-03-30 | v1.46.58 | UX | Added global :focus-visible keyboard focus rings — overrides inline outline:none with !important so keyboard users always have a visible indicator | `index.css:171` | — |
| 2026-03-30 | v1.46.59 | Fix | Desktop now opens to Inbox — Home had no sidebar nav entry; users were stranded on a page with no return path | `App.jsx:57` | — |
| 2026-03-30 | v1.46.60 | Fix | Added missing UPDATE RLS policy on feeds table — folder assignment/renaming/settings all silently blocked | `supabase-schema.sql:79` | — |
| 2026-03-30 | v1.46.61 | Perf | Removed HomePage from bundle — 32KB dead chunk; unreachable since v1.46.34; default route → InboxPage | `App.jsx:18,217,229` | — |
| 2026-03-30 | v1.46.62 | Polish | Added aria-label to ActionBtn — icon-only article action buttons now have proper accessible names | `FeedItem.jsx:66` | — |
| 2026-03-30 | v1.46.63 | Feature | Cards inline editing — click annotation to edit in place, × to remove tags, inline input to add tags | `CardsPage.jsx:17-41,99-179` | — |
| 2026-03-30 | v1.46.64 | Fix | isRSSUrl matches query-param feed URLs (?rss=1, ?format=rss, ?type=rss) — Slickdeals-style feeds no longer misdetected as articles | `fetchers.js:561` | — |
| 2026-03-30 | v1.46.65 | Feature | Articles-per-feed limit defaults to 20 (was 80), configurable 10/20/50 in Settings → Reading | `fetchers.js:190-193,156,202, SecondaryPages.jsx:728` | — |
| 2026-03-30 | v1.46.66 | Fix | RSS image/description extraction — nodeName iteration replaces unreliable CSS namespace selectors; description falls back to content:encoded | `fetchers.js:268,281-329` | — |
| 2026-03-30 | v1.46.67 | Nav | Added Review to mobile BottomNav (replacing Today) — Review was inaccessible on mobile despite being a primary screen | `BottomNav.jsx:17-28` | — |
| 2026-03-30 | v1.46.68 | Perf | Lazy-load LoginPage — removed from eager main bundle; authenticated users never load this chunk | `App.jsx:7,17,186` | — |
| 2026-03-30 | v1.46.70 | Perf | Add canonical link to index.html — fixes Lighthouse SEO score 0.91 → 1.0 | `index.html:23` | — |
| 2026-03-30 | v1.46.71 | Perf | Lazy-load PWAInstallBanner — removed from eager main bundle; only loads on beforeinstallprompt | `App.jsx:7,32,326` | — |
| 2026-03-30 | v1.46.72 | UX | Remove borderBottom from skeleton rows — skeleton now matches real feed items (no dividers), eliminates border flash on load | `InboxPage.jsx:1160,1195` | — |
| 2026-03-30 | v1.46.73 | Nav | BottomNav hides on scroll-down, reappears on scroll-up — 54px more content visible while reading; resets on navigation | `BottomNav.jsx:40-52`, `InboxPage.jsx:897` | — |
| 2026-03-30 | v1.46.74 | Polish | Fade-in on page navigation — key={page} remounts page wrapper, triggering 150ms fadeIn from index.css | `App.jsx:257` | — |
| 2026-03-30 | v1.46.75 | Fix | Image extraction: filter data: URI placeholders, check data-src/data-lazy-src, scan all imgs not just first — fixes SlickDeals thumbnails | `fetchers.js:310-323` | — |
| 2026-03-30 | v1.46.76 | Polish | Remove decorative 1px divider in Sidebar between nav and feed tree — whitespace separates sections sufficiently | `Sidebar.jsx:394` | — |
| 2026-03-30 | v1.46.77 | Polish | Reduce mobile list-item padding 13px→10px — matches skeleton, eliminates height-shift on load, shows ~1 extra item per screen | `FeedItem.jsx:287` | — |
| 2026-03-31 | v1.46.78 | Polish | Remove "Smart"/"Feeds" 9px uppercase section labels from Sidebar — chrome reduction, add-buttons retained with tooltips | `Sidebar.jsx:401,418` | — |
| 2026-03-31 | v1.46.79 | Polish | Scale list-view items ~10% larger — thumbnail, title font, and row padding increased on both mobile and desktop | `FeedItem.jsx:212,231,236,260,287,315,344,373` | — |
| 2026-05-14 | v1.46.169 | Polish | BottomNav Cards/Review icons replaced with custom SVGs matching Sidebar — CreditCard and RefreshCw were semantically wrong | `BottomNav.jsx:2-14,22` | — |
| 2026-05-22 | v1.46.181-182 | Fix | ReviewPage rating buttons bottom padding increased to clear fixed BottomNav pill on mobile | `ReviewPage.jsx:251` | — |
| 2026-05-22 | v1.46.183 | Feature | Desktop Bazqux-style compact list + infinite scroll — 60 items initial, +40 on scroll near bottom; J/K expands; default view changed to list | `FeedItem.jsx:344-424`, `InboxPage.jsx:42,69,274,290,929,985` | — |
| 2026-05-22 | v1.46.184 | Perf | PodcastPlayer: RAF loops throttled 60fps→2fps, preload=auto, auto-play on episode set | `PodcastPlayer.jsx:31,292,308,368,545` | — |
| 2026-05-22 | v1.46.185 | Fix | Restore desktop list thumbnails (lg default), newArticleCount decrements on scroll-read, remove DigestModal | `FeedItem.jsx:344`, `InboxPage.jsx:43,447`, `DigestModal.jsx` deleted | — |
| 2026-06-08 | v1.46.234 | Perf | Lazy-load @mozilla/readability — removes 34 kB (11 kB gz) from InboxPage initial load | `fetchers.js:2,425` | — |
| 2026-06-08 | v1.46.235 | Polish | Page transition: scale+opacity → pure opacity fade, 180ms→100ms — snappier nav, removes GPU composite layer | `index.css:55-58`, `App.jsx:310` | — |
| 2026-06-08 | v1.46.236 | Polish | FeedItem read-state opacity fade 250–300ms→150ms — mark-read feels 2× faster in all views | `FeedItem.jsx:229,307,361,409` | — |
| 2026-06-08 | v1.46.237 | Feature | Today page redesigned: Queue + Feed Pulse + Reading Log — replaces magazine grid; 20kB→9kB | `TodayPage.jsx`, `App.jsx:236` | — |
| 2026-06-08 | v1.46.238 | Feature | Persistent podcast mini-player — desktop modal → floating mini-bar bottom-right; navigate freely while listening | `PodcastPlayer.jsx:296,547-639` | — |

---

## How to use this log

**During a session:** The `/iterate` skill appends a row here automatically after each deploy.

**End of session:** Run `/cost` in the Claude Code chat. It shows total tokens used this session. Note the dollar amount in the "Session Cost" column for the iterations you just ran. Then stop — all changes are committed and deployed.

**Resuming later:** Just open Claude Code and type `/iterate`. It reads git history, CLAUDE.md, and memory to understand current state — no need to brief it.

## Session history

| Date | Iterations | Focus | Session Cost |
|------|-----------|-------|-------------|
| 2026-05-26 | 1 | Feature: Anki-style Review + Supabase SR persistence + Cards due status + scroll fix + FeedItem perf + Reeder-style mobile density | — |
| 2026-03-27 | 2 | Nav: label consistency, touch cancel fix | — |

## Security Audit & Fixes (2026-06-26)

**P0 Deployed (v1.46.466):**
- Fixed XSS vulnerability in ContentViewer.jsx (removed dangerouslySetInnerHTML)
- Added feed URL validation in supabase.js (whitelist http/https)
- Clarified API key docs (.env.example) — server-side only

**Outstanding:**
- npm audit: 26 vulnerabilities (1 low, 20 moderate, 5 high)
- `npm audit fix --force` introduced Vite breaking change — needs careful resolution in separate branch
- SECURITY_AUDIT.md generated with full details of 5 issues and remediation roadmap

## Mobile UI/UX Audit — P1 Fixes (2026-07-05)

Full UI/UX audit vs Feedly/Inoreader/BazQux found no popstate/back-button handling anywhere in the app (Android back or edge-swipe-back exits the app instead of closing overlays), three sub-16px inputs triggering iOS Safari auto-zoom, and a manifest.json locking orientation to portrait with a stray unused splash color. All three fixed below.

| Date | Version | Area | Change | Files | Session Cost |
| 2026-07-05 | v1.46.467 | UX | Back button/gesture now closes topmost open overlay (article, drawer, search, modals) via LIFO history stack instead of exiting the app | `lib/backStack.js`, `hooks/useBackButtonClose.js`, `App.jsx`, `InboxPage.jsx`, `ContentViewer.jsx` | — |
| 2026-07-05 | v1.46.467 | Fix | Bumped sub-16px input font sizes to 16px to stop iOS Safari auto-zoom on focus | `TagsInput.jsx:73`, `AddModal.jsx:272,360,398`, `SecondaryPages.jsx:961` | — |
| 2026-07-05 | v1.46.467 | Fix | Removed portrait orientation lock from PWA manifest; corrected splash background/theme color to match actual dark theme | `manifest.json` | — |

## Broken Production Deploy — Lock File Fix (2026-07-10)

GitHub Actions deploys for v1.46.466 and v1.46.467 both failed silently at `npm ci` (package-lock.json out of sync with package.json — missing `esbuild@0.28.1` and related entries, likely left over from an abandoned `npm audit fix --force` attempt noted in the 2026-06-26 security audit). Local builds always succeeded because `npm run build` doesn't re-validate the lock file, so this went unnoticed for two releases. Regenerated the lock file and verified `npm ci` passes.

| 2026-07-10 | v1.46.468 | Fix | Regenerated package-lock.json to resolve npm ci failure that broke the last two GitHub Actions deploys | `package-lock.json` | — |

v1.46.468's lockfile regen didn't fix CI — `npm ci` still failed with the same "Missing: esbuild@0.28.1" error against the identical commit. Root cause: `vitest ^4.1.2` requires `vite ^6/^7/^8` as a peer dependency; since the project pins `vite ^5.3.1`, npm nested a private `vite@8.1.0` (whose peer `esbuild` range is `^0.27.0 || ^0.28.0`) under `node_modules/vitest`, and that peer edge never got a concrete resolved entry in the lockfile — reproducible regardless of which npm/Node version generated the lock. Fixed by pinning `vitest`/`@vitest/coverage-v8` to `^3.2.7`, the latest 3.x line whose `vite` dependency range (`^5.0.0 || ^6.0.0 || ^7.0.0-0`) actually includes our installed `vite@5.3.1`.

| 2026-07-10 | v1.46.469 | Fix | Downgraded vitest 4.1.2 → 3.2.7 (and @vitest/coverage-v8 to match) to resolve the real vite-peer mismatch causing broken GitHub Actions deploys since v1.46.466 | `package.json`, `package-lock.json` | — |

## /iterate — Cards page stray character (2026-07-10)

Every build logged an esbuild warning ("The character `}` is not valid inside a JSX element") pointing at `CardsPage.jsx`, but the build never failed because JSX tolerates a bare `}` as literal text rather than erroring. Traced it to a stray orphaned `)}` left over from when the card delete button was switched from conditional-on-hover rendering to always-mounted-with-opacity-toggle — the old closing tokens were never removed, so a literal `)` was being rendered next to the delete button on every card.

| 2026-07-10 | v1.46.470 | Fix | Removed orphaned `)}` in CardsPage.jsx that rendered a stray `)` character next to the card delete button and triggered an esbuild warning on every build | `CardsPage.jsx:809` | — |

## /iterate — Performance run, podcast player rebuild (2026-07-10, 5-iteration run)

User requested 5 consecutive /iterate loops focused specifically on performance, targeting the podcast player/features for rebuild. Iteration 1 below; more to follow in this same session.

| 2026-07-10 | v1.46.471 | Perf | Podcast seek bar + mini-bar progress RAF loops now stop while paused instead of polling at 60fps for the entire mounted lifetime; resume on next play event | `PodcastPlayer.jsx` (SeekBar ~L30-63, mini-bar ~L321-350) | — |
| 2026-07-10 | v1.46.472 | Perf | Podcast position-save interval now skips the localStorage write while paused (position isn't changing, so periodic writes during idle sessions were pure waste) | `PodcastPlayer.jsx:~375-385` | — |
| 2026-07-10 | v1.46.473 | Fix | Desktop podcast mini-bar progress strip was fixed at width:0% while only its transform was updated, so it never visibly showed progress; matched to mobile's width:100%+transformOrigin:left pattern | `PodcastPlayer.jsx:~679-682` | — |
| 2026-07-10 | v1.46.474 | Perf | parseRSSItem walked each item's DOM subtree twice per item (image extraction + iTunes duration/creator lookup); consolidated to one shared querySelectorAll("*") call, halving per-item traversal on every feed refresh | `fetchers.js:~248-310` | — |
| 2026-07-10 | v1.46.475 | Perf | Podcast seek bar now seeds its duration display from the RSS feed's already-known iTunes duration instead of showing 0:00 until real audio metadata loads over the network | `PodcastPlayer.jsx` (parseDuration + SeekBar) | — |

5-iteration performance run on podcast features complete (v1.46.471–475): eliminated wasted RAF polling while paused (2 loops), eliminated redundant localStorage writes while paused, fixed a desktop-only rendering bug where progress never visibly displayed, halved per-item RSS parsing DOM traversal, and improved perceived duration display latency. All verified via npm test + npm run build + green GitHub Actions before each deploy.

## Podcast playback regression — rollback (2026-07-11)

User reported "tapping play does nothing" shortly after the 5-iteration run above. Line-by-line re-review of all 5 diffs found no logic bug — `audio.play()`, the enclosure/audioUrl detection, and the play/pause click handlers were untouched by any of the 5 changes. Browser extension wasn't connected, so live console/network inspection wasn't possible. Reverted `PodcastPlayer.jsx` and `fetchers.js` to the pre-run baseline (v1.46.470) as a bisection step: if this fixes playback, the bug is in one of the 5 changes and will be re-isolated by re-applying them one at a time; if it doesn't, the cause is unrelated (e.g. audio host/CORS, autoplay policy) and investigation continues elsewhere.

| 2026-07-11 | v1.46.476 | Fix | Reverted all 5 podcast perf changes (v1.46.471-475) back to v1.46.470 baseline after user reported broken podcast playback; cause not yet confirmed | `PodcastPlayer.jsx`, `fetchers.js` | — |

## /iterate — 6-loop run, performance + minimal UI (2026-07-11)

User requested 6 iterations focused on performance and UI minimalism. Given the unresolved podcast regression above (root cause still unconfirmed despite thorough static review, and no browser access available to verify runtime behavior), this run deliberately avoids core interactive wiring (article open/read/save handlers, click paths) in favor of changes that are functionally inert by construction — pure display memoization and CSS-only simplification — so a repeat of that incident is structurally less likely.

| 2026-07-11 | v1.46.477 | Perf | FeedItem ListItem+CardItem: memoized readingTime() computation (was splitting full article text on every render, unmemoized) | `FeedItem.jsx:306-309,529-532` | — |
| 2026-07-11 | v1.46.478 | Perf | FeedItem mobile ListItem: memoized preview-text HTML/whitespace stripping (was two regex replace() calls on every render, unmemoized) | `FeedItem.jsx:311-318` | — |

User explicitly chose to proceed with the highest-risk fix (touching article open/read/save click handlers) despite no browser access to verify live, given the podcast regression earlier this run. Implemented via a new memoized `FeedItemRow` wrapper component that derives stable per-item callbacks using a "latest handler via ref" pattern (`handlersRef`) — mirrors the existing `markReadFnRef` convention already used in this file. Traced every click-routing branch (multi-select toggle, card vs. list open behavior incl. `setCursorIdx` ordering, mark-read/unread toggle, long-press start/cancel) against the original inline closures to confirm exact behavioral equivalence before deploying.

| 2026-07-11 | v1.46.479 | Perf | Fixed FeedItem memo() being defeated by per-render closure props in InboxPage — new memoized FeedItemRow wrapper with ref-based stable callbacks; rows no longer re-render on unrelated InboxPage state changes | `InboxPage.jsx:35-81,196(handlersRef),968-977(ref sync),1446-1460,1480-1495` | — |

User clarified "minimal UI" means less visual noise (flatter, calmer surfaces) rather than less information density or leaner code — remaining iterations target decorative effects like backdrop-filter blur, which is both visual clutter and expensive to composite.

| 2026-07-12 | v1.46.480 | Polish/Perf | Flattened BottomNav pill — removed backdrop-filter blur+saturate glass effect, solid background + softer shadow | `BottomNav.jsx:94-111` | — |
| 2026-07-12 | v1.46.481 | Polish/Perf | Flattened ContentViewer reader header — removed backdrop-filter blur(14px), solid T.bg background | `ContentViewer.jsx:442-446` | — |
| 2026-07-12 | v1.46.482 | Polish | Flattened CardsPage "New Card" modal backdrop — removed blur(6px), plain dim overlay (opacity bumped .45→.55 to compensate) | `CardsPage.jsx:461-463` | — |

6-iteration run complete (v1.46.477–482): 3 pure-performance memoization fixes (readingTime, preview text, and the FeedItem memo()-defeat root cause), plus 3 "less visual noise" flattening passes removing backdrop-filter blur from BottomNav, ContentViewer header, and CardsPage modal. Noted but intentionally left alone: `HomePage.jsx` is fully dead code (not imported anywhere) — a cleanup candidate for a future "leaner codebase" pass, not this run's "visual noise" goal since it's never rendered. Also left `PodcastPlayer.jsx` untouched this run pending confirmation on the still-open playback regression from the prior 5-iteration run.

## Podcast playback start latency (2026-07-13)

User reported podcasts take a long time to start playing, confirmed the delay is the same whether entering via the feed-list mini-player (`PodcastPlayer.jsx`) or the full-reader inline player (`ContentViewer.jsx`'s `PodcastEpisodeView`) — pointing at a bottleneck shared by both, not player-specific logic. No browser connection was available to capture a live network waterfall, so rather than touch either player's playback code again (the last podcast "perf" pass broke playback on unverified static reasoning alone), the fix targets the one thing both paths provably share: neither begins any connection to the audio host until the user taps play, paying a full DNS/TCP/TLS handshake at that moment. Added a `<link rel="preconnect">` warmed as soon as a podcast episode renders in the feed list, deduped per host origin — purely additive, touches zero existing audio/play/pause logic, so risk of a repeat regression is structurally near-zero.

| 2026-07-13 | v1.46.483 | Perf | Preconnect to a podcast's audio host as soon as its episode is visible in the feed list, so the mini-player and full-reader players both skip a cold handshake when the user taps play | `FeedItem.jsx:1-22,681-684` | — |

| 2026-07-13 | v1.46.484 | UX | Reeder-style UI refresh: shared SHAPE token scale; floating pill bottom nav (blur reinstated by design); roomier feed rows with uniform unread dot; sidebar pill active states; reader line-height 1.68 + unified radii; fixed invalid bottom-nav border color | `tokens.js:172`, `BottomNav.jsx:95`, `FeedItem.jsx:351`, `Sidebar.jsx:59`, `ContentViewer.jsx:798`, `index.css:161` | — |

| 2026-07-14 | v1.46.484 | UX | Reeder refresh round 2: modals, mobile sheets, podcast mini-players, reader panels, and UI primitives aligned to SHAPE scale; added shadowFloatUp/shadowFloatLeft directional tokens; search-header stray shadow removed at judge stage | `tokens.js:186`, `AddModal.jsx:185`, `MobileFeedDrawer.jsx:196`, `PodcastPlayer.jsx:516`, `NotePanel.jsx:48`, `UI.jsx:10` | — |

| 2026-07-14 | v1.46.484 | UX | Reeder refresh round 3: all six page files on SHAPE scale — flat hairline cards, accentSurface hovers, pill progress bars, grouped settings; judge fixed one hover-color inconsistency (ReadLater grid) | `CardsPage.jsx`, `ReviewPage.jsx:507`, `TodayPage.jsx`, `HomePage.jsx`, `ReadLaterPage.jsx:333`, `SecondaryPages.jsx` | — |

| 2026-07-14 | v1.46.485 | Feature | Readwise-inspired card upgrades: Shuffle spotlight (CardsPage), Mastered retire action (ReviewPage), session-size setting; judge fixes: dead Escape handler (focus), unified fb-review-count vs legacy quota key | `CardsPage.jsx:574`, `ReviewPage.jsx:43,219`, `SecondaryPages.jsx:699` | — |

| 2026-07-14 | v1.46.485 | Feature | Virtual Zettelkasten: zettel.js connection engine + 8 unit tests (Haiku-built), Connected cards traversal in spotlight, Related themes trail, synthesis nudge on Review | `src/lib/zettel.js`, `src/test/zettel.test.js`, `CardsPage.jsx:78,293,670`, `ReviewPage.jsx:496` | — |

| 2026-07-14 | v1.46.486 | Feature | Backlinks: expanded cards in theme lists show Connected cards inline, tap-to-traverse via spotlight; computation gated to the single expanded card | `CardsPage.jsx:918-925,1030-1063,363` | — |

| 2026-07-15 | v1.46.487 | Feature | Theme map: Map/Grid toggle on Cards, SVG graph of themes (nodes=card count, edges=co-occurrence), zero-dep seeded force layout + 8 tests; judge escalation fixed weak spring force + multi-word-tag separator in own spec | `src/lib/graphLayout.js`, `src/test/graphLayout.test.js`, `src/components/ThemeGraph.jsx`, `CardsPage.jsx:10,56,798,840` | — |

| 2026-07-15 | v1.46.487 | UX | Theme map polish: activeTag node highlight, tappable edges with shared-cards preview modal (Escape pattern reused); judge extended lastTheme tracking to all navigation paths | `ThemeGraph.jsx:9,72`, `CardsPage.jsx:65,731,913` | — |

| 2026-07-16 | v1.46.488 | Feature | Connected cards on Review: reuses zettel.js relatedHighlights() to show up to 3 related cards beneath tags on the revealed flashcard, read-only (no navigation, linear queue); orchestrator run — Sonnet worker built the diff, QA (Haiku) caught missing deps/changelog before deploy | `ReviewPage.jsx:8,198-201,524-554` | — |

| 2026-07-17 | v1.46.489 | UX | Reeder-style mobile menus round 1: article overflow menu as mobile bottom sheet, new "More" tab + sheet (History/Stats/Settings) replacing drawer quick-nav, swipe-left "Mark read" on feed drawer rows; orchestrator run — 3 parallel/sequential Sonnet workers (2 disjoint-file groups + 1 dependent), all accepted on first pass; QA (Haiku) caught stale changelog before deploy | `ContentViewer.jsx:1131`, `BottomNav.jsx:37-41,162-181`, `MoreMenuSheet.jsx` (new), `MobileFeedDrawer.jsx:16-113,296-320`, `App.jsx:38,76,82-83,330,388,392-397` | — |

| 2026-07-17 | v1.46.490 | Fix | ErrorBoundary crash-screen theme fallback compared against capitalized theme names that never matched real lowercase storage values, always rendering dark regardless of user's actual theme; fixed to map all 6 themes from token values; orchestrator run (2x /orchestrate loop, Haiku workers) — judge caught worker missed `handleRetryFeed` producing items without `_ts` during the companion perf fix, patched directly | `UI.jsx:3,115-124` | — |
| 2026-07-17 | v1.46.490 | Perf | InboxPage filteredItems memo re-parsed article dates via `new Date()` on every filter/search recompute despite a precomputed `_ts` field already existing on every item; switched 4 call sites to read `_ts` directly, plus fixed `handleRetryFeed` (found by judge) which built items without `_ts`, silently sinking retried-feed articles to the bottom of every sort | `InboxPage.jsx:398,422,450,453,654-659` | — |
| 2026-07-17 | v1.46.491 | Fix | Article reader's Aa font-panel and desktop "···" menu were clipped/invisible, absolutely-positioned inside the header's `overflow:hidden` auto-hide wrapper; converted both to viewport-fixed positioning computed from the trigger's bounding rect; orchestrator run — Sonnet worker, accepted first pass | `ContentViewer.jsx:81-82,516-529,1168-1169,1217-1224,1233` | — |
| 2026-07-17 | v1.46.491 | UX | Reader line-width switched from fixed px to ch units (60/70/85ch) so line length stays in the reading-comfort range regardless of font size; article body reworked to render real `<p>` paragraphs (matching the existing translated-text pattern) across plain/bionic/highlighted branches instead of one flat span; judge caught `paragraphGroups` not filtering empty groups, one-line fix applied during integration | `readerPrefs.js:33`, `index.css:5`, `ContentViewer.jsx:904-996` | — |
| 2026-07-17 | v1.46.492 | Perf | Inbox search debounced (150ms) so the full filter/sort pipeline no longer reruns on every keystroke; split the item-filtering memo so saving/unsaving no longer forces an unrelated full recompute of the main feed view (savedItems was only needed by the "Saved" branch but shared as a dependency across both); orchestrator run — Sonnet worker, accepted first pass, QA (Haiku) confirmed no stale deps | `InboxPage.jsx:375-471` | — |
| 2026-07-17 | v1.46.493 | Feature | Bulk operations on Cards: Select mode with checkboxes + bulk delete/add-tag in the theme-detail view, mirroring Inbox's multi-select bar; new `deleteHighlights()` bulk helper; orchestrator run — Sonnet worker, judge caught real bug (TagsInput's keydown/focus path wasn't blocked by the click-capture guard, allowing tag mutation during select mode) and a stale-editingId visual glitch, both fixed during integration | `CardsPage.jsx:67-149,314-354,423-429,1102-1236`, `supabase.js:160-163` | — |
| 2026-07-19 | v1.46.494 | Feature | Add-source modal now normalizes schemeless input (`nytimes.com` → `https://nytimes.com`) before type-detection/discovery, so typing a bare website name finds its feed instead of silently failing `new URL()`; added an inline hint on genuine discovery failure instead of silent no-op; orchestrator run — Sonnet worker, judge caught the separate "Save for later" button still using the raw unnormalized URL, fixed during integration | `AddModal.jsx:16-19,104-172,393-399,436` | — |
| 2026-07-19 | v1.46.495 | UX | Reeder-style swipe polish: haptic feedback fires the instant a swipe crosses its action threshold (article rows + feed rows), and feed rows in the Feeds drawer gained swipe-right-to-unsubscribe alongside the existing swipe-left-to-mark-read; orchestrator run — parallel Haiku (haptic) + Sonnet (unsubscribe swipe) workers, both accepted first pass, QA confirmed no regressions | `FeedItem.jsx:167,178`, `MobileFeedDrawer.jsx:17,20-24,38-58,133,204,381,391`, `App.jsx:389` | — |
| 2026-07-19 | v1.46.496 | Fix | Dead-code audit: deleted a stray duplicate lockfile + 3 orphaned files (NotesPage.jsx/ArticleNotesPanel.jsx from the removed article-notes feature, HomePage.jsx superseded by Today) with zero references anywhere; wired up the orphaned HistoryPage (fully built, never routed) with a case in App.jsx + a desktop Sidebar nav entry using an already-defined-but-unused icon; aligned navigation.test.js's page-id list with real routing; orchestrator run — 3 parallel workers, judge caught the test fix still missing "stats", fixed during integration | `App.jsx:30,253`, `Sidebar.jsx:589-595`, `navigation.test.js:10-21,28`, 4 files deleted | — |
| 2026-07-19 | v1.46.497 | UX | Bottom nav bar gained text labels under each icon (native tab bar convention) while keeping the established floating blurred-pill treatment; orchestrator run — Sonnet worker, orchestrator verified visually via a headless-Chromium screenshot at 320px viewport since this was a layout-risk change (7 icons already tight at that width) — labels fit cleanly, no clipping | `BottomNav.jsx:158,179,207` | — |

| 2026-07-20 | v1.46.498 | Polish | Overscroll containment on all five scrollable areas inside fixed overlays (Feeds drawer, highlights drawer, search overlay, reader scroll container, transcript panel) so list-end scrolling no longer chains to the page behind; fixed bottom-nav pill max-width overshoot (294→280px at 320px viewport) by trimming icon-chip padding 1px/side; done directly by orchestrator (<30 lines), verified via headless-Chromium measurement + screenshot | `MobileFeedDrawer.jsx:365`, `HighlightsDrawer.jsx:44`, `MobileSearchOverlay.jsx:193`, `ContentViewer.jsx:681,1421`, `BottomNav.jsx` (3 chips) | — |

| 2026-07-20 | v1.46.499 | Feature | Smoothness batch: customizable swipe actions (fb-swipe-right/left prefs + Settings selects, defaults byte-identical to prior hardcoded mapping), periodic background sync registration at boot (completes dead sw.js handler), content-visibility on Inbox rows (zero-dep virtualization); orchestrator run — Haiku x2 + Sonnet workers in parallel on disjoint files, all accepted first pass; rich-HTML rendering remains gated on user's sanitizer-dependency decision | `main.jsx:23-33`, `FeedItem.jsx:121-215`, `SecondaryPages.jsx:717-758`, `InboxPage.jsx:67` | — |

| 2026-07-20 | v1.46.500 | UX | Reverted bottom-nav text labels per user request — icon-only pill restored byte-identical to pre-label state (label spans removed, chip padding back to 6px 8px); direct orchestrator edit, pure revert to previously-shipped state | `BottomNav.jsx:155,175,196` | — |

| 2026-07-20 | v1.46.501 | Perf | Dead-code removal: deleted 10 verified-unreferenced exported functions across 4 lib files (supabase.js notes/updateFeedName/getAllArticleTags, apiKeys setAiProvider, exportUtils allHighlightsToMarkdown, readerPrefs applyBionicToText) — ~120 lines, deletion-only, build+test green; orchestrator run — Haiku worker with build-gate verification, judge confirmed deletion-only numstat + all kept exports intact | `supabase.js`, `apiKeys.js`, `exportUtils.js`, `readerPrefs.js` | — |

| 2026-07-20 | v1.46.501 | Perf | ContentViewer reading-progress bar decoupled from React render — scroll no longer triggers ~100 re-renders/article; bar painted imperatively via ref (transform kept out of React style; per-render useLayoutEffect re-syncs pre-paint to defeat re-render reset), only the 8% scroll-to-top FAB uses boolean state; done directly by orchestrator (behavior-sensitive hot path), validated in headless Chromium harness (tracks scroll + survives forced re-render, no flash) | `ContentViewer.jsx:1,86-99,196-201,289-291,427-433,842` | — |
| 2026-08-05 | v1.46.502 | Fix | Atom podcast feeds now detected — audio enclosure (`link[rel=enclosure]` per Atom spec, vs RSS 2.0's dedicated `<enclosure>` element) and itunes:duration parsed on Atom entries, matching existing RSS 2.0 podcast support; verified with a jsdom sanity check against a sample Atom feed | `fetchers.js:251-281` | — |
| 2026-08-05 | v1.46.503 | Feature | Continuous podcast playback — playing an episode builds a queue from sibling episodes of the same show already loaded in InboxPage's `allItems`; PodcastPlayer shows "Up next" and a Next button, auto-advances via the audio `ended` event (ref-tracked to avoid stale closures); verified with 5 React Testing Library cases (up-next label, auto-advance fires/doesn't fire, Next button click + disabled state) | `App.jsx:68-69,88-97,374`, `InboxPage.jsx:47-64,797-803,1007`, `PodcastPlayer.jsx:266-284,287,303-308,324-327,507,634` | — |
| 2026-08-05 | v1.46.504 | Feature | Full "Up Next" queue view — the "Up next: X" line is now a button that opens the whole queue (mobile: swaps the full-screen player into a list view; desktop: swaps the expanded panel) with jump-to-play, move up/down reorder, and remove-from-queue per episode; App.jsx owns queue mutation (`handlePlayFromQueue` drops skipped episodes, `handleRemoveFromQueue`, `handleReorderQueue`); verified with `npm run build` + `npm test` (no dedicated component test harness for drag-free reorder UI) | `App.jsx:95-113,374`, `PodcastPlayer.jsx:275-329,436-441,498-503,566-651,702-772` | — |
| 2026-07-15 | v1.46.497 | UX | Review de-quizzed: reveal step removed, Readwise-style immediate full-card flow, buttons renamed Next/Show-again-soon, SM-2 untouched; judge added BUTTON guard to global keydown (Enter on Mastered/nudge no longer hijacked) | `ReviewPage.jsx:236-248,426-577` | — |

| 2026-07-16 | v1.46.497 | Perf | Blur 20px→14px on floating chrome (GPU); adaptive force-layout iterations (O(n²) cap); audit confirmed windowing/lazy/preconnects already optimal; Lighthouse rerun stopped, baseline not refreshed | `tokens.js:181`, `graphLayout.js:41-57` | — |

| 2026-08-02 | n/a (native/, not deployed) | Feature | /orchestrate Phase 1 of native iOS+macOS rewrite: xcodegen scaffold (iOS 18/macOS 15, SwiftData+CloudKit dual-write direction per SWIFT_REBUILD.md update), Theme.swift ported from tokens.js (6 themes found, not 3), SwiftData models for feeds/highlights/saved/read_items/feed_folders (article_tags dropped — highlight-level tags only, per decision), NavigationSplitView shell with Inbox/Reader/Cards/Review/Today. Both platforms build clean | `native/project.yml`, `native/FeedboxCore/Sources/FeedboxCore/*`, `native/Feedbox/Views/*`, `SWIFT_REBUILD.md` | — |

| 2026-08-03 | n/a (native/, not deployed) | Feature | Native Phase 2a: RSSService (FeedKit 10.x, RSS/Atom/JSON via FeedKit.Feed(remoteURL:), source snapshot as Sendable FeedSource to satisfy Swift 6 strict concurrency since SwiftData models aren't Sendable), FeedItem live-fetch model (not persisted, mirrors web's feedCache.js approach), wired ModelContainer (was missing entirely — @Query was previously non-functional app-wide), InboxView (add-feed sheet, concurrent multi-feed fetch, tap-to-toggle-read via ReadItem, empty state) routed into NavigationShell. Verified in iOS Simulator screenshot, both platforms build clean | `native/FeedboxCore/Sources/FeedboxCore/Services/RSSService.swift`, `native/FeedboxCore/Sources/FeedboxCore/Models/FeedItem.swift`, `native/FeedboxCore/Sources/FeedboxCore/Models/FeedSource.swift`, `native/FeedboxCore/Package.swift`, `native/Feedbox/FeedboxApp.swift`, `native/Feedbox/Views/InboxView.swift`, `native/Feedbox/Views/NavigationShell.swift` | — |

| 2026-08-03 | v1.46.497 | Polish | Real Feedbox brand mark (traced from official artwork) replaces generic RSS-arc icon in favicon.svg, PWA/app icons (192/512/apple-touch), sidebar collapsed glyph, and public/feedbox-logo.png (notification/PWA-banner icon); added fixed-color `BRAND` tokens decoupled from theme accents | `public/favicon.svg`, `public/icons/*`, `public/feedbox-logo.png`, `src/lib/tokens.js`, `src/components/FeedboxMark.jsx`, `src/components/Sidebar.jsx:471` | — |
| 2026-08-03 | v1.46.497 | Fix | Login screen was still branded "BrainBits" (purple gradient BB mark) instead of Feedbox — swapped in the real logo mark, brand-teal tile, and correct app name | `src/pages/LoginPage.jsx:41-58` | — |
| 2026-08-03 | n/a (native/, not deployed) | Feature | Native Phase 2c: FeedDiscoveryService (SwiftSoup `<link rel=alternate>` scraping + common-path probing, mirrors web's `discoverFeed`/fetchers.js minus the CORS proxy) so Add Feed accepts a plain website URL, not just a raw feed URL; InboxView add-feed sheet now shows discovery progress and an inline error on failure | `native/FeedboxCore/Sources/FeedboxCore/Services/FeedDiscoveryService.swift`, `native/Feedbox/Views/InboxView.swift` | — |
| 2026-08-03 | n/a (native/, not deployed) | Chore | Xcode project switched from unset manual signing (DEVELOPMENT_TEAM empty, un-buildable on device) to automatic signing on the existing personal Apple ID team (3CQYDR2JLH), to install debug builds on a paired physical iPhone | `native/Feedbox.xcodeproj/project.pbxproj` | — |
| 2026-08-03 | n/a (native/, not deployed) | Feature | Native Phase 2b: ArticleService (SwiftSoup 2.6.x extraction — Readability equivalent per SWIFT_REBUILD.md Option A), ReaderWebView (cross-platform WKWebView wrapper, iOS UIViewRepresentable / macOS NSViewRepresentable), ReaderView renders extracted article HTML themed to the active palette via inline CSS (Color.resolve(in:) → rgba). Inbox rows now NavigationLink into Reader (was toggle-read-on-tap); manual read/unread toggle moved to swipe action; Reader auto-marks read on load. Known gap: extracted bodyHTML rendered via loadHTMLString without an HTML sanitizer allow-list — script tags are stripped but inline event-handler attributes aren't; acceptable for a single-user MVP, flagged for hardening before wider distribution. Both platforms build clean, relaunched in Simulator without crash | `native/FeedboxCore/Sources/FeedboxCore/Services/ArticleService.swift`, `native/FeedboxCore/Sources/FeedboxCore/Models/ArticleContent.swift`, `native/FeedboxCore/Package.swift`, `native/Feedbox/Views/ReaderView.swift`, `native/Feedbox/Views/ReaderWebView.swift`, `native/Feedbox/Views/InboxView.swift` | — |
| 2026-08-04 | v1.46.497 | UX | Web login regains passwordless email magic-link alongside GitHub/Google — supersedes the 2026-06-23 OAuth-only simplification (v1.46.443) per explicit user decision, now shared with the native app's sign-in flow | `src/lib/supabase.js`, `src/hooks/useAuth.jsx`, `src/pages/LoginPage.jsx` | — |
| 2026-08-04 | n/a (native/, not deployed) | Feature | Native Reader highlight capture: JS-injected floating "Highlight" button on WKWebView text selection (WKScriptMessageHandler bridge, works on iOS + macOS variants), HighlightSheet collects note/tags, saved as a SwiftData `Highlight` | `native/Feedbox/Views/ReaderWebView.swift`, `native/Feedbox/Views/ReaderView.swift`, `native/Feedbox/Views/HighlightSheet.swift` | — |
| 2026-08-04 | n/a (native/, not deployed) | Feature | Native Cards view: highlights grouped by tag (multi-tag highlights appear under each tag, untagged under "Untagged"), row → detail push, empty state matching InboxView's pattern; wired into NavigationShell's `.cards` case | `native/Feedbox/Views/CardsView.swift`, `native/Feedbox/Views/NavigationShell.swift` | — |
| 2026-08-04 | n/a (native/, not deployed) | Feature | Native magic-link auth bridge: SupabaseBridgeService (URLSession/PostgREST, no SDK) requests the magic link and exposes `read_urls` push/pull; AuthSession stores the JWT in Keychain and gates NavigationShell behind sign-in; SignInView + `.onOpenURL` parses the `feedbox://auth-callback` redirect; added `keychain-swift` to FeedboxCore's Package.swift. Manual follow-ups still needed: SUPABASE_URL/SUPABASE_ANON_KEY in Info.plist, `feedbox://` URL scheme registration, and adding the redirect URL to Supabase's allow-list | `native/FeedboxCore/Sources/FeedboxCore/Services/SupabaseBridge.swift`, `AuthSession.swift`, `SupabaseConfig.swift`, `native/Feedbox/Views/SignInView.swift`, `native/Feedbox/FeedboxApp.swift`, `native/FeedboxCore/Package.swift` | — |
| 2026-08-04 | n/a (native/, not deployed) | Chore | Registered CardsView.swift, HighlightSheet.swift, and SignInView.swift in project.pbxproj (PBXBuildFile/PBXFileReference/group/Sources phase) — this project doesn't use file-system-synchronized groups, so new Swift files silently fail to compile unless added manually. Full `xcodebuild -scheme Feedbox -destination 'platform=macOS'` build verified green after registration | `native/Feedbox.xcodeproj/project.pbxproj` | — |
| 2026-08-04 | n/a (docs) | Chore | Split the changelog in two — top-level `CHANGELOG.md` stays web-only since `deploy.sh` auto-promotes its `[Unreleased]` section to a real web version number on every deploy; native isn't on that pipeline, so it now has its own `native/CHANGELOG.md`. Moved native-only entries over and trimmed the shared magic-link entry down to its web-scoped half, with a cross-reference to the native file. `AGENT_LOG.md` stays the one shared internal log across both apps | `CHANGELOG.md`, `native/CHANGELOG.md` (new) | — |
| 2026-08-06 | v1.46.497 | Chore | Local checkout had drifted 12 commits behind origin/main (a separate session had pushed v1.46.489–496 in the meantime); rebased the branding/login/perf/review-UX batch onto the real origin/main and re-versioned from the stale v1.46.488 to v1.46.497 before deploying | `CHANGELOG.md`, `AGENT_LOG.md`, `package.json` | — |
| 2026-08-07 | v1.46.498 | Polish | Deduped 3 drifted folder-color maps (Sidebar, Manage Feeds, an inline copy) into one `FOLDER_COLORS` export in tokens.js — the same folder previously showed different teal/amber hex depending on page; orchestrator run — Sonnet worker, accepted first pass | `tokens.js:200`, `Sidebar.jsx:6,9-10`, `SecondaryPages.jsx:18,1280` | — |
| 2026-08-07 | v1.46.498 | Polish | BottomNav 7-tab pill overshot its max-width by ~4px at 320px viewports, spilling icons past the rounded border; trimmed icon-wrapper padding 1px/side (6px 8px → 6px 7px) for 10px margin; orchestrator run — Sonnet worker verified the overflow arithmetic against live code before fixing, accepted first pass | `BottomNav.jsx:155,175,195` | — |
| 2026-08-07 | v1.46.498 | UX | Modal/drawer close buttons (Folder, Smart Feed, Highlights drawer, PWA install banner) bumped 28–32px → 44×44px tap target per iOS/WCAG guidance; orchestrator run — Haiku worker, mechanical, accepted first pass | `FolderModal.jsx:51`, `SmartFeedModal.jsx:73`, `PWAInstallBanner.jsx:91`, `HighlightsDrawer.jsx:63` | — |
| 2026-08-07 | v1.46.498 | Fix | ErrorBoundary hardcoded ~10 Nocturne-theme hex values, so a crash always rendered dark regardless of the user's actual theme; now resolves `fb-theme` from localStorage against tokens.js's 6 theme exports (try/catch guarded, falls back to Nocturne) — component stays a class since it's a real error boundary; orchestrator run — Sonnet worker, accepted first pass | `ErrorBoundary.jsx:2-19,48-118` | — |
| 2026-08-07 | v1.46.498 | Perf | Refreshed Lighthouse baseline (prior datapoint was ~4 months stale) — Performance 99, Accessibility 100, Best Practices 100, SEO 92, confirming no regression from the polish batch above | `scripts/perf-history.json` | — |
| 2026-08-07 | n/a (native/, not deployed) | Security | Closed the ArticleService HTML-sanitizer gap flagged 2026-08-03 — extracted bodyHTML now runs through SwiftSoup `Cleaner`/`Whitelist.relaxed()` before reaching `ReaderWebView.loadHTMLString`, stripping inline event-handler attributes and blocking `javascript:` URLs (script/style tag removal alone didn't cover attribute-based XSS); added a regression test proving both vectors are closed while normal links/content survive; done directly (self), not delegated — security-sensitive code per CLAUDE.md routing rules; `swift test` 19/19 passing | `FeedboxCore/Sources/FeedboxCore/Services/ArticleService.swift`, `FeedboxCore/Tests/FeedboxCoreTests/ArticleServiceTests.swift` | — |
| 2026-08-07 | n/a (native/, not deployed) | Chore | Wired 2 of 3 remaining manual native-auth follow-ups from 2026-08-04: `SUPABASE_URL`/`SUPABASE_ANON_KEY` and the `feedbox://` URL scheme now come from a literal `Feedbox/Info.plist` (xcodegen-generated from `project.yml`'s new `info.properties`, replacing `GENERATE_INFOPLIST_FILE` since custom keys + a URL-scheme dict-array aren't expressible as flat `INFOPLIST_KEY_*` settings); verified `pbxproj`'s existing signing settings matched `project.yml` before regenerating so `xcodegen generate` wouldn't silently revert the 2026-08-03 manual-signing fix. Third item — adding `feedbox://auth-callback` to Supabase's redirect allow-list — is dashboard-only and left for the user (no Supabase CLI/API access from this environment). macOS build verified clean; iOS build target resolves but couldn't run — no iOS Simulator runtime installed in this environment | `native/project.yml`, `native/Feedbox/Info.plist` (new), `native/Feedbox.xcodeproj/project.pbxproj` | — |
| 2026-08-07 | v1.46.499 | Polish | Sidebar's expanded header now shows the Feedbox mark next to the wordmark, matching the collapsed-state badge — favicon, PWA/iOS icons, and login screen already used the real mark, this was the one spot showing plain text | `Sidebar.jsx:467-475` | — |
| 2026-08-07 | v1.46.499 | Chore | Repo housekeeping: removed two confirmed-dead tracked files (`public/admin/index.html`, `landing/`) superseded by their own commit history; cleared ~900MB of local-only clutter (`.DS_Store`, stray `node_modules 2/`, Swift `.build/` cache); added `native/` and `coverage/` to the shared `.gitignore` (previously only excluded via local, unshared `.git/info/exclude`) | `.gitignore`, `public/admin/index.html` (removed), `landing/` (removed) | — |
| 2026-08-10 | n/a (native/, not deployed) | Polish | Native app had no AppIcon at all (default blank icon) — created `native/Feedbox/Assets.xcassets/AppIcon.appiconset` from the real Feedbox brand mark (`public/favicon.svg` traced source, rendered via `rsvg-convert` at 1024×1024, ink `#2F373B` background flattened opaque per Apple's no-alpha requirement), full iOS universal + macOS 1x/2x size set; wired `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon` into `project.yml` (not just pbxproj, per the signing-drift lesson above) and regenerated. Confirmed on-device after 3 build attempts killed mid-CodeSign while the Mac's screen was locked (codesign can't reach an unlocked Keychain) — succeeded immediately once the user was back at the machine | `native/Feedbox/Assets.xcassets/AppIcon.appiconset/*`, `native/project.yml` | — |
| 2026-08-10 | n/a (dashboard config) | Fix | Magic-link "can't connect to local server" traced to Supabase Auth's `site_url` — still the default `http://localhost:3000` from initial project creation, never updated; any redirect not exactly matching `uri_allow_list` silently falls back to it. User generated a Supabase personal access token (CLI login was non-TTY-blocked in this environment, worked via `supabase login --token`); used the Management API (`PATCH /v1/projects/{ref}/config/auth`) rather than `supabase config push` to avoid clobbering the 240+ other auth fields (OAuth secrets, mailer templates, etc.) a full local `config.toml` push would've overwritten blind. Set `site_url` to `https://rss.brainbits.us` and added `feedbox://auth-callback` to `uri_allow_list` — closes the native-auth redirect item pending since 2026-08-04 | Supabase dashboard (Auth config), no repo files | — |
| 2026-08-09 | n/a (native/, not deployed) | Fix | First real device deploy of the native app, end to end. Found the 2026-08-03 automatic-signing fix had been silently reverted by the 2026-08-07 `xcodegen generate` run (the fix was a hand-edit to the generated `project.pbxproj`, never captured in `project.yml`'s own `CODE_SIGN_STYLE: Manual`/empty `DEVELOPMENT_TEAM`, so it didn't survive regeneration) — moved the fix into `project.yml` itself (`CODE_SIGN_STYLE: Automatic`, `DEVELOPMENT_TEAM: 3CQYDR2JLH`) so it's regeneration-proof; installed the iOS 26.5 platform (Xcode had none); `xcodebuild ... -allowProvisioningUpdates build` then succeeded (auto-provisioned `Apple Development: ptdz@pm.me`). Installed to the paired iPhone via `xcrun devicectl device install app` — first attempt failed with CoreDeviceError 4000 (device disconnected immediately) while the phone was locked, succeeded once unlocked. First launch was blocked by iOS with `FBSOpenApplicationServiceErrorDomain`/"profile has not been explicitly trusted" until the user trusted the dev cert at Settings → General → VPN & Device Management; launched clean after that. App runs on device now | `native/project.yml`, `native/Feedbox.xcodeproj/project.pbxproj` | — |
| 2026-08-10 | n/a (docs) | Chore | `native/` re-audit (Sonnet worker, parallel to the GitHub sign-in work above) against the 2026-08-04 `BACKLOG.md` snapshot — corrected 2 stale items (highlight sync to Supabase is actually done via `HighlightSyncService`, wired at `ContentView.swift:18`; CloudKit gap still accurate) and found 2 new P0s by reading the real code rather than trusting the old list: the sidebar's "Reader" item is selectable but dead-ends (`NavigationShell.swift` has no `.reader` case in its detail switch), and `RSSService.fetchAll` silently swallows every per-feed fetch error with zero user-facing signal. Also flagged: no search anywhere in the native app, `SavedItem` model registered but never surfaced in any view. All findings spot-checked directly (grep) before being folded into `BACKLOG.md` — full list there | `native/BACKLOG.md` | — |
| 2026-08-10 | n/a (native/, not deployed) | Fix | Closed both P0s from the same-day `native/BACKLOG.md` audit: (1) sidebar "Reader" item dead-ended into an empty placeholder — added `AppSection.sidebarCases` excluding `.reader`, `NavigationShell` sidebar list now uses it, existing Inbox/Cards → Reader `NavigationLink` path untouched; (2) `RSSService.fetchAll` silently swallowed per-feed fetch failures — changed its return type from `[FeedItem]` to `FetchAllResult(items:failedSources:)`, `InboxView` now shows a warning banner naming failed feeds (using existing `palette.warning` token). Both self-implemented (API-shape + UX judgment, no dedicated native agent). Verified: `swift test` 31/31, `xcodebuild` clean on macOS + iOS Simulator. Device install to the paired iPhone (PDMAX (2)) blocked — this session's Xcode has no Apple ID in Accounts and no cached provisioning profile for `com.feedbox.app`; needs a one-time manual sign-in (Xcode → Settings → Accounts) before `-allowProvisioningUpdates` can auto-provision | `native/Feedbox/Views/AppSection.swift`, `NavigationShell.swift`, `InboxView.swift`, `native/FeedboxCore/Sources/FeedboxCore/Services/RSSService.swift`, `RSSServiceTests.swift` | — |
| 2026-08-12 | n/a (native/, not deployed) | Feature | Search (Inbox + Cards, `.searchable()` over already-loaded in-memory data) and swipe-to-tag in Cards. Caught a real correctness gap before shipping swipe-to-tag: `HighlightSyncService`'s doc comment asserted a highlight's fields "never change after creation," so a naive tag-edit feature would've silently never synced to Supabase after first sync (local/remote drift). Fixed by adding a targeted `SupabaseBridgeService.pushHighlightTags` (PATCH, not the POST-upsert `pushHighlight` uses) called directly at edit time, and updated the sync service's doc comment to explain why re-tagging doesn't go through the reconciler. Self-implemented (no dedicated native agent). Verified: `swift test` 31/31, `xcodebuild` clean on macOS + iOS Simulator. Device install still blocked — Xcode Accounts still has no Apple ID as of this entry (phone itself is fine, `available (paired)` over wireless) | `native/Feedbox/Views/InboxView.swift`, `CardsView.swift`, `native/FeedboxCore/Sources/FeedboxCore/Services/SupabaseBridge.swift`, `HighlightSyncService.swift` | — |
| 2026-08-11 | n/a (native/, not deployed) | Feature | Reader typography controls (top P0 from the Reeder comparison) — new `ReaderPreferences.swift` (font choice: Theme/Serif/Sans-Serif, size 14-28pt, line-spacing 1.3-2.2x, `UserDefaults`-backed, deliberately kept separate from `Theme.readerFont`) + a new "Aa" settings sheet in `ReaderView`'s toolbar, wired into `renderedHTML`'s CSS. Self-implemented (no dedicated native agent). Verified: `swift test` 31/31, `xcodebuild` clean on macOS + iOS Simulator. Also: launched Xcode for the user to sign into Accounts (Apple ID) — still pending, confirmed still blocking device install as of this entry (no `IDEProvisioningTeams` default set, no cached provisioning profile, and the iPhone dropped to `unavailable` in `devicectl`) | `native/FeedboxCore/Sources/FeedboxCore/Models/ReaderPreferences.swift` (new), `native/Feedbox/Views/ReaderView.swift` | — |
| 2026-08-11 | n/a (docs) | Chore | Reeder comparison (Sonnet worker, parallel to the AI/TTS work above), researched via live WebSearch against both current Reeder products (Reeder Classic vs. the new subscription Reeder) rather than stale pretrained assumptions, cross-checked against `native/BACKLOG.md`/`CHANGELOG.md` so already-tracked/shipped items weren't double-counted. Top finding (spot-checked, confirmed correct — `ReaderView.swift` hardcodes `font-size: 19px` and a fixed per-theme font with no user control): Reader typography controls is the highest-leverage gap, now P0. Also added: Shortcuts actions, swipe-to-tag in Cards (P1), Bionic Reading/mark-as-read-on-scroll/OPML import (P2). Flagged Reeder's unified social/video/podcast timeline and public tag-feeds as deliberately out of scope per `CLAUDE.md`'s non-goals. Full detail in new `native/REEDER_COMPARISON.md`, top items folded into `native/BACKLOG.md` | `native/REEDER_COMPARISON.md` (new), `native/BACKLOG.md` | — |
| 2026-08-11 | n/a (native/, not deployed) | Feature | On-device article summaries (Apple Intelligence) + read-aloud, both self-implemented (new framework surface, no dedicated native agent). Bumped `native/project.yml` + `Package.swift` deployment target iOS 18→26/macOS 15→26 for `FoundationModels` (user chose this over a hybrid on-device+server-fallback option after being shown the device-support trade-off); also needed `swift-tools-version: 6.0`→`6.2` for `PackageDescription`'s `.v26` constant. New `AIService.swift` (`SystemLanguageModel`/`LanguageModelSession`, gated on `.availability` with per-reason UI copy) and `SpeechService.swift` (`AVSpeechSynthesizer`, best-available Premium/Enhanced voice — literal Siri voice isn't exposed to third-party apps, corrected that expectation before building). Every unfamiliar API (FoundationModels availability enum cases, LanguageModelSession init/respond signature, AVSpeechSynthesisVoiceQuality cases, AVSpeechSynthesizerDelegate optionality) was verified directly against this machine's installed SDK `.swiftinterface`/header files before writing code, not guessed from pretrained knowledge — every guess turned out correct on first compile. `ArticleContent` gained `plainText` (SwiftSoup-stripped) for both features to consume. Verified: `swift test` 31/31, `xcodebuild` clean on macOS + iOS Simulator | `native/project.yml`, `native/FeedboxCore/Package.swift`, `native/FeedboxCore/Sources/FeedboxCore/Services/AIService.swift` (new), `SpeechService.swift` (new), `Models/ArticleContent.swift`, `native/Feedbox/Views/ReaderView.swift` | — |
| 2026-08-10 | n/a (native/, not deployed) | Feature | Native GitHub sign-in — new `OAuthSession` (`ASWebAuthenticationSession` wrapper, `@MainActor` with `nonisolated` `presentationAnchor` bridged via `MainActor.assumeIsolated` for Swift 6 strict concurrency) opens `{SUPABASE_URL}/auth/v1/authorize?provider=github&redirect_to=feedbox://auth-callback`; `SignInView` gained a "Continue with GitHub" button above the existing magic-link form. Reuses the existing `parseCallback`/`AuthSession.store` path unchanged — same redirect URL already allow-listed in Supabase since the 2026-08-10 magic-link fix, so no dashboard change was needed. No new SPM dependency (`AuthenticationServices` is a system framework). Done directly (self), not delegated — security-sensitive auth code per CLAUDE.md routing rules. Verified: `xcodebuild` clean on both `platform=macOS` and `platform=iOS Simulator,name=iPhone 17 Pro`; `swift test` 30/30 passing. Not verified: an actual on-device GitHub OAuth round-trip (needs a live GitHub account + browser interaction). Orchestrator run — a parallel Sonnet worker audited `native/` for a separate improvement backlog (see entry below) | `native/FeedboxCore/Sources/FeedboxCore/Services/OAuthSession.swift` (new), `native/Feedbox/Views/SignInView.swift`, `native/CHANGELOG.md` | — |
| 2026-08-13 | v1.46.500 | Fix | Article scrolling felt buggy — a tap-to-select-sentence gesture in the highlight toolbar checked only touch distance at release, so a momentum-scroll flick landing within 15px of its start (common with rubber-banding) was misread as a stationary tap, force-selecting the nearest sentence and popping a floating toolbar over the article mid-scroll. Fixed by tracking `touchmove` and cancelling the tap-to-select path on any real finger movement. Self-implemented (single-file, <30 lines, under the orchestrator's skip-ceremony threshold) | `src/components/SelectionToolbar.jsx:23-96` | — |
| 2026-08-13 | v1.46.500 | Feature | Orchestrator run: six Reeder-style Inbox view-option controls — Display favicons, Grayscale favicons (disabled when favicons are off), Dim archived articles, Sort order (Newest/Oldest/Unread-first/Smart, replacing the old standalone Smart-sort toggle with no orphaned state), Group by (None/Date/Feed), list Font style (Serif/Sans-serif, reusing the reader's existing font stacks). Added to both the mobile Display sheet and desktop View-options popover; all persist via the existing per-pref localStorage pattern. Routed to `frontend` agent (Sonnet, clear spec) in parallel with the scroll fix above (disjoint files); one REVISE cycle — judge review caught that `groupBy: "feed"` clustering only applied to the main Inbox memo, so picking it while on the Saved page produced repeated feed headers instead of one clean group per feed; worker extracted the clustering into a shared `clusterByFeed()` helper used by both memos. `npm test` 33/33 and `vite build` verified clean after the fix | `src/pages/InboxPage.jsx`, `src/components/FeedItem.jsx` | — |
| 2026-08-13 | n/a (native/, device install pending) | Feature | Brought native iOS `InboxPreferences`/`SettingsView` to parity with the web view-options work above: added Grayscale Favicons, Dim Archived Articles (gates the leading-visual opacity dim that was previously unconditional, default on to preserve existing behavior), Unread First sort, and a new Font picker (Sans-serif/Serif) for row titles — the font control needed genuinely new plumbing since `InboxView` rows had no font-family customization before (`ReaderPreferences.fontChoice` only ever touched the reader's rendered HTML, not SwiftUI list rows). Self-implemented, no dedicated native agent exists per CLAUDE.md's routing table. Verified: `swift test` 31/31, `xcodebuild -destination 'generic/platform=iOS' -allowProvisioningUpdates` clean. Device install not attempted — `xcrun devicectl` showed the paired iPhone as `unavailable` (locked/asleep); user chose build-only over unlocking it this session | `native/FeedboxCore/Sources/FeedboxCore/Models/InboxPreferences.swift`, `native/Feedbox/Views/InboxView.swift`, `native/Feedbox/Views/SettingsView.swift` | — |
| 2026-08-13 | n/a (native/, not deployed) | Chore | Native version tracking had silently stalled at `MARKETING_VERSION: 0.1.0` since project inception — every one of ~20 shipped features (search, swipe-to-tag, reader typography, on-device AI summaries, read-aloud, GitHub sign-in, highlight capture, Cards, Review, Today, magic-link auth, HTML sanitizer fix, and more) sat under a single never-closed `[Unreleased]` in `native/CHANGELOG.md` because native has no TestFlight/App Store pipeline to force a version bump. Bumped to `MARKETING_VERSION: 0.2.0` / `CURRENT_PROJECT_VERSION: 2` in `project.yml`, regenerated via `xcodegen generate` (no drift), promoted the entire accumulated Unreleased list plus this session's settings-parity work into `## [0.2.0] — 2026-08-13`, and documented the going-forward convention at the top of the changelog: bump by hand on each meaningful batch of work rather than letting Unreleased grow indefinitely | `native/project.yml`, `native/Feedbox.xcodeproj/project.pbxproj`, `native/CHANGELOG.md` | — |
| 2026-08-13 | v1.46.501 | Nav | `/iterate` round: browser tab title fell back to the generic "Feedbox" on Manage Feeds, History, and the legacy Notes→Cards redirect — `PAGE_TITLES` only covered 8 of 10 reachable page ids. Added the missing entries so every page gets an accurate, specific tab/history title | `App.jsx:124` | — |
| 2026-08-14 | v1.46.502 | Nav | `/iterate` round 2: mobile had no direct path to Manage Feeds (desktop's always-visible "Sources" sidebar item) — only reachable via a conditional link in Settings that vanished with zero feeds. Added a "Manage Feeds" row to the mobile More sheet with the same feed-error badge desktop shows | `MoreMenuSheet.jsx:15-19,89-98`, `App.jsx:432` | — |
| 2026-08-17 | v1.46.503 | Fix | Orchestrator run, sub-task 1 of 4 (self, <30 lines, skip-ceremony): live smoke-audit found Manage Feeds 100% crashed in production — `freshCount` computed inside `ManageFeedsPage`'s stats `useMemo` but never returned, so the "Fresh Feeds" tile threw `ReferenceError` on every render | `SecondaryPages.jsx:1521-1533` | — |
| 2026-08-17 | v1.46.503 | Fix | Orchestrator run, sub-task 2 of 4 (self, <30 lines): Cards header showed "2 highlights · 2 themes" while 6 highlights actually existed — `totalTagged` silently excluded untagged highlights from the displayed count. Swapped to `highlights.length` (true total) and removed the now-dead `totalTagged` memo | `CardsPage.jsx:265-267,537` | — |
| 2026-08-17 | v1.46.503 | Fix | Orchestrator run, sub-task 3 of 4: live audit found article text rendering literal HTML entities (`&#8217;` instead of ’) — `fullText` (the Reader's actual content) was assigned raw XML `.textContent` with zero entity-decoding, unlike the short `description` preview which already decoded via `stripHtml`. Routed to `backend` (Sonnet); worker caught and corrected an error in my brief (`fullText` is HTML-consumed in one dead code path, not pure plain text — verified harmless via `grep -rn bodyHtml src/` showing zero live reads) rather than blindly following it. Extracted `decodeHtmlEntities` helper, `stripHtml` now delegates to it, applied at all 3 `fullText` call sites | `fetchers.js:178,272,307,397-409` | — |
| 2026-08-17 | v1.46.503 | Feature | Orchestrator run, sub-task 4 of 4: built Read Aloud (TTS) with live word-highlight sync — did not exist anywhere in the web app despite CLAUDE.md's architecture doc describing it as already built (stale documentation, not a regression; the separate native iOS app has its own unrelated `AVSpeechSynthesizer`-based read-aloud). Routed to `frontend` (Sonnet). New `useReadAloud.js` hook wraps native `speechSynthesis`/`SpeechSynthesisUtterance` in sentence-sized chunks (Chrome drops utterances past ~15s) with a generation counter guarding against stale `onboundary`/`onend` callbacks after Stop/Play; `ContentViewer.jsx` gained Play/Pause/Stop controls and a word-span render path in `HighlightedText`, active only while speaking so idle reading renders byte-identical to before (confirmed via diff: net 2 lines removed in the whole file, both from the untouched-logic comment/signature). Word highlight uses `T.accent`, distinct from the 4 saved-highlight colors. Judged and accepted after reading the full diff — not just the worker's summary — plus `npm test`/`vite build`; live browser verification was not possible (local dev requires sign-in, no authenticated session on `localhost`, would not attempt OAuth without explicit user permission). Known disclosed trade-offs: saved highlights aren't clickable during active playback, and Bionic Reading mode is silently ignored while speaking — both narrow, non-crashing, reverts on Stop | `hooks/useReadAloud.js` (new), `ContentViewer.jsx:19,107,590-622,879-880,955-1052` | — |
| 2026-08-17 | native v0.2.1 (not deployed) | Feature | Orchestrator run: native iOS/macOS Read Aloud gained live word-highlight sync, closing the direct parity gap with the web feature built earlier this session. Checked the web entity-decoding bug's native equivalent first — not present, native uses `FeedKit`+`SwiftSoup` (real parsers, single consistent extraction path) rather than the web's dual-path skip-decode bug. Routed to a general-purpose Sonnet worker with an explicit brief covering a technical constraint I found by reading the code first: the reader is a `WKWebView`, and naively driving word-highlight through the `html` prop would reload the whole page on every word-boundary event. Worker split `ReaderWebView.updateUIView`/`updateNSView` into reload-only-when-html-changed + always-apply-highlight-via-`evaluateJavaScript`, added `AVSpeechSynthesizerDelegate.willSpeakRangeOfSpeechString` to `SpeechService` with correct UTF-16-safe `NSRange`→word-index mapping. One REVISE cycle: `pause()` was clearing the highlight, unlike web's `pause()` which keeps it as a "where was I" cue — fixed in one line. Verified independently at every step (not just trusted the worker's reports): read the actual diffs, reran `swift test` (31/31) and the device `xcodebuild` build (BUILD SUCCEEDED) myself both before and after the revision. Bumped `MARKETING_VERSION` 0.2.0→0.2.1 per the versioning convention set up earlier this session | `FeedboxCore/Services/SpeechService.swift`, `Feedbox/Views/ReaderWebView.swift`, `Feedbox/Views/ReaderView.swift`, `native/project.yml` | — |
| 2026-08-17 | native v0.2.2 (installed on device) | Fix | User reported native reader scrolling as "janky/stuttery." Root-caused directly (no worker needed, single-file, <10 lines): `ReaderWebView.applyHighlight` — added the previous session for TTS — fired `evaluateJavaScript` unconditionally on every `updateUIView` call, including idle re-renders triggered by the scroll-direction chrome-hide toggle, unlike the already-correct `reloadIfNeeded`/`lastLoadedHTML` guard sitting right next to it. Added a matching `lastAppliedWordIndex` diff-guard. Verified: `swift test` 31/31, device build succeeded, then installed and launched on the paired iPhone to confirm | `Feedbox/Views/ReaderWebView.swift` | — |
| 2026-08-17 | native v0.2.2 (installed on device) | Feature | Orchestrator run: rebuilt native's AI article-summary feature for web parity, per user request (button under the hero image, matching visual style, persisted so it "keeps it part of the article"). Prior state: toolbar button → one-off `.sheet` → ephemeral, regenerated every open. Researched web's exact implementation first (button placement/styling, Anthropic Haiku call shape, two-tier persistence — always-on localStorage plus a Supabase side-effect only when the article is separately saved) via a dedicated research agent, then made and stated explicit scope decisions before dispatching: kept native's on-device Apple Intelligence backend (`AIService`) unchanged — "similar to web app" read as UX parity, not an architecture swap; persist unconditionally rather than replicating web's save-gated tier, since native's `SavedItem` isn't wired to anything yet; skipped web's multi-style switcher and follow-up Q&A input as not requested. New `ArticleSummary` SwiftData model (keyed by URL) plus a new JS bridge (`#fb-summary` DOM region patched via `evaluateJavaScript`, reusing the reload-avoidance/state-diffing pattern from the same day's TTS and scroll-jank work) so reopening an article with a cached summary shows the result card immediately, no flash of the button first, no full-page reload on any state transition. Two independent escaping layers verified by reading the code: JSON-encoded JS string literal (Swift→JS hop) plus HTML-escaping on the DOM write (both sides), correct `&` before `<`/`>` ordering confirmed on both the Swift and JS copies. Routed to a general-purpose Sonnet worker; accepted after independently re-running `swift test` (31/31) and the device build (BUILD SUCCEEDED) myself, not just trusting the report. Bumped `MARKETING_VERSION` 0.2.1→0.2.2 covering both this and the scroll fix above, then installed to the paired iPhone | `FeedboxCore/Models/ArticleSummary.swift` (new), `Feedbox/FeedboxApp.swift`, `Feedbox/Views/ReaderView.swift`, `Feedbox/Views/ReaderWebView.swift`, `native/project.yml` | — |
| 2026-08-17 | native v0.3.0 (installed on device) | Feature | Orchestrator run, phase 1 of 2 (user approved the 3-part plan with "let's do it"): Manage Feeds redesigned (unread summary row, folder-grouped collapsible sections using the `Feed.folder`/`Folder.feeds` relationship that already existed in the schema, real favicons via the same key-free Google service the web app uses) and Save/unsave wired up for the first time (`SavedItem` had existed since an earlier session with zero UI anywhere) via a bookmark swipe action + a new three-way All/Unread/Saved bottom bar replacing the old single unread-toggle. Dispatched as two parallel Sonnet workers on disjoint files (`ManageFeedsView.swift` vs `InboxView.swift`) since both were independently scoped and verifiable. Judged each independently: read both full diffs (not just the reports), spot-verified a claimed `palette.amber.text` token actually exists in `Theme.swift` before trusting it, reran `swift test` (31/31) and a combined integrated device build (BUILD SUCCEEDED) myself covering both sub-tasks together. OPML import (phase 2, shares `ManageFeedsView.swift` with this phase so was sequenced after) dispatched next. Bumped `MARKETING_VERSION` 0.2.2→0.3.0 (minor — new user-facing capability) and installed to the paired iPhone mid-session per explicit "Deploy to iPhone" request, ahead of phase 2 landing | `Feedbox/Views/ManageFeedsView.swift`, `Feedbox/Views/InboxView.swift`, `native/project.yml` | — |
| 2026-08-17 | native v0.3.1 (installed on device) | Feature | Orchestrator run, phase 2 of 2: OPML import dispatched to a Sonnet worker (new `OPMLImportService` using Foundation `XMLParser`, no dependency; folder/feed duplicate-avoidance by name/URL; parse-fully-before-any-write so a bad file never touches SwiftData) — judged by tracing the parser's stack-based folder-nesting logic by hand through a doubly-nested-folder case before accepting, then independently reran `swift test` (37/37, incl. 6 new) and the device build. In parallel, self-implemented (single file, well-understood, no need for a worker round-trip) three small Inbox polish items requested via a shared reference screenshot across several short follow-up messages, each confirmed via `AskUserQuestion` before touching already-shipped code since they changed behavior, not just style: search moved to a nav-bar magnifying-glass icon (`.searchable(placement: .toolbar)`); the phase-1 All/Unread/Saved segmented bar restyled into a star/pill/menu icon row (Saved as a dedicated toggle, Unread as a binary pill, Sort/Group menu moved down from the top toolbar — removed `InboxFilterMode`'s now-dead `title`/`systemImage`/`CaseIterable` rather than leave unused code); feed-grouped section headers now render as pill badges. Bumped `MARKETING_VERSION` 0.3.0→0.3.1 and installed to the paired iPhone | `FeedboxCore/Services/OPMLImportService.swift` (new), `FeedboxCore/Tests/OPMLImportServiceTests.swift` (new), `Feedbox/Views/ManageFeedsView.swift`, `Feedbox/Views/InboxView.swift`, `native/project.yml` | — |
| 2026-08-17 | native v0.3.2 (installed on device) | Feature | User uploaded a screen recording asking to "copy the bottom nav view and actions" — no video-reading tool available, so extracted 14 evenly-spaced frames myself via a small AVFoundation/Swift script (`ffmpeg` wasn't installed) and read them as images. Identified the actual pattern: an animated Saved/Unread/All 3-way filter on the reference app's *Feeds* screen (not Inbox, where we'd already built a simpler version) — selected mode expands into a labeled pill, others collapse to icons, and every feed row's count changes with the selected filter. Confirmed scope via `AskUserQuestion` (full behavior vs. visual-only vs. wrong screen) since a wrong guess would've wasted a full round-trip. Full behavior required linking `SavedItem` back to its originating feed — added a native-only `feedID: UUID?` field (not part of the web `saved` table schema, flagged in a doc comment so a future sync implementation doesn't get confused) — did this small, well-understood 2-file change myself, then dispatched the larger animated-filter-bar + OPML-export work (both landing in `ManageFeedsView.swift`, so combined into one worker to avoid a file conflict) — OPML export was a second ask that arrived mid-turn ("let's review and ship OPML export and import"). Judge review caught a real bug before accepting: the export `ShareLink` computed its file inline in the toolbar body, causing a synchronous disk write on every re-render including mid-animation — same bug class as the reader-scroll-jank fix earlier this session. One REVISE cycle fixed it (cached in `@State`, regenerated only via `.task`/`.onChange(of: feeds/folders)`). Verified independently at every step: read all the code, not just the reports; reran `swift test` (39/39) and the device build myself before and after the revision. Bumped `MARKETING_VERSION` 0.3.1→0.3.2 and installed to the paired iPhone | `FeedboxCore/Models/SavedItem.swift`, `Feedbox/Views/InboxView.swift`, `FeedboxCore/Services/OPMLExportService.swift` (new), `FeedboxCore/Tests/OPMLExportServiceTests.swift` (new), `Feedbox/Views/ManageFeedsView.swift`, `native/project.yml` | — |
| 2026-08-18 | v1.46.504 (pending deploy) | UX | User uploaded a screen recording ("I've shared in the session and the one I'm touching right now") after a `/clear` wiped the earlier screenshot from context — flagged the gap and asked them to resend rather than guessing; no video-reading tool available, so installed `ffmpeg` via Homebrew and extracted 18 frames myself. Identified the reference as a full-screen "Feeds" list (not an article inbox): big title + synced-at timestamp, collapsible Folders/Feeds sections, and a Starred/Unread/All bottom segmented filter that recomputes every count shown above it — closely matching a design already shipped on the native iOS side per an earlier `native v0.3.2` log entry using the same reference app. Confirmed scope via `AskUserQuestion` (target: full-screen page vs. restyled sheet vs. new route; filter: functional vs. visual-only) since a wrong guess would cost a full rewrite cycle. Rebuilt `MobileFeedDrawer.jsx` from a bottom sheet into a full-screen overlay: unread counts reuse the existing `feedUnreadCounts` prop, "all" counts read per-feed cached article totals via `getCachedFeed`, "starred" counts fetch `getSaved(user.id)` and bucket by matching `source` to each feed's display name (best-effort — the `saved` table has no `feed_id`). Folders are now drill-down only (tap navigates in) instead of expanding inline. Dispatched to a Sonnet frontend worker with a fully self-contained spec (data sources, exact row/section behavior, what to preserve — the swipe-to-mark-read/unsubscribe gesture, the Smart Feeds section). Judged by reading the full diff myself (not the worker's summary): verified `getSaved`'s `source` field actually exists in `saveItem`'s upsert shape, confirmed `readlater`/`history`/`all` are all real page keys in `App.jsx`, confirmed `SHAPE.radiusSm`/`radiusPill` exist — then reran `npm test` (33/33) and `npm run build` independently rather than trusting the worker's report, reverting the git-tracked `dist/` output afterward to keep the tree clean | `src/components/MobileFeedDrawer.jsx`, `src/App.jsx` | — |
| 2026-08-19 | native v0.3.4 (installed on device) | Fix | User asked to push the latest native work to the iPhone; before building, checked the actual installed version via `devicectl device info apps` and found it reported 1.0 (1) despite `project.yml`/`native/CHANGELOG.md` having tracked `MARKETING_VERSION`/`CURRENT_PROJECT_VERSION` up through 0.3.4 (9) across many prior sessions — root cause: the 2026-08-07 switch from `GENERATE_INFOPLIST_FILE` to a literal `Info.plist` (needed for `SUPABASE_URL`/URL-scheme keys) never carried `CFBundleShortVersionString`/`CFBundleVersion` into the new plist's `properties`, so every version bump since then was cosmetic only — the shipped binary's Info.plist had no version keys at all. Fixed by adding both as `$(MARKETING_VERSION)`/`$(CURRENT_PROJECT_VERSION)` placeholders to `project.yml`'s `info.properties` (Xcode substitutes build settings into custom Info.plists at build time); verified the generated plist and the installed device app both now correctly show 0.3.4 (9). Also wrote `native/deploy.sh` per the user's request to save tokens on future deploys — one command (`xcodegen generate` → signing-drift check per the `project.yml`-is-source-of-truth lesson → `xcodebuild` targeted at whichever device `devicectl` reports connected → `devicectl install` → prints installed version), tested end-to-end twice against the real paired iPhone. No feature work landed this session, no version bump needed | `native/project.yml`, `native/deploy.sh` (new), `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.4.0 (installed on device) | Feature | `/orchestrate` run: "revamp the inbox with a more procreate-like feel with each feed showing its new count." Asked the user up front which platform (web vs. native iOS) since they're separate codebases — native iOS only, confirmed via `AskUserQuestion`. Self-implemented at Fable tier per CLAUDE.md's escalation rule (no dedicated native agent exists, and a single cohesive visual redesign is a bad fit for splitting across fresh-context workers). Read `InboxView.swift`/`ManageFeedsView.swift`/`Theme.swift` first to reuse existing tokens (`Shape.radiusCard`, `feedItemsStore.unreadCount`) rather than invent new ones. Extracted the prior flat-article-list `InboxView` into a new `InboxArticleListView(feedFilter: Feed?)` (byte-identical behavior when filter is nil), then rebuilt `InboxView` itself as a 2-column `LazyVGrid` gallery of feed tiles — latest-article-image thumbnail or initial-avatar fallback, unread-count badge, spring press-bounce — plus a pinned "All Items" tile routing to the same extracted view. No new SwiftData models, preferences, or dependencies. Adversarial self-review caught a real bug before building: I'd written `FeedboxApp.sharedModelContext` (a hallucinated API) in `addFeed()` after trimming `InboxView`'s properties — fixed by restoring the real `@Environment(\.modelContext)`. Verified: `swift test` 39/39, `xcodebuild` clean on iOS device + iOS Simulator; macOS build separately fails on a pre-existing, unrelated issue in `ManageFeedsView.swift` (`EditButton`/`.bottomBar` unavailable on macOS) — flagged, not fixed, out of scope for this iOS-only request. Couldn't screenshot-verify the live layout with real data: the Simulator has no signed-in session (fresh install lands on `SignInView`, confirmed via screenshot) and `devicectl` has no screenshot subcommand for the physical device — installed straight to the paired iPhone (which has the real session/feeds) via `native/deploy.sh` for the user to review directly. Bumped `MARKETING_VERSION` 0.3.4→0.4.0 (new user-facing capability) | `native/Feedbox/Views/InboxView.swift` (rewritten), `native/Feedbox/Views/InboxArticleListView.swift` (new), `native/project.yml` | — |
| 2026-08-19 | native v0.4.1 (installed on device) | Feature | User asked to (1) rebuild Read Aloud to only use "iOS Siri voices" and (2) change the just-shipped Inbox gallery from cards to a list. Before touching TTS, verified the literal-Siri-persona constraint independently rather than trust the existing code comment — dumped this Mac's full `AVSpeechSynthesisVoice.speechVoices()` catalog (180 voices) via a throwaway script, confirmed no identifier or name contains "Siri" at any quality tier, so the only real lever is restricting to Enhanced/Premium ("Siri-quality") voices. Found a real gap while reading the existing code: `SettingsView`'s footer already claimed the Voice picker only offered Enhanced/Premium, but `SpeechPreferences.availableVoices` had no quality filter at all — every novelty voice (Fred, Zarvox, Eloquence family) was actually selectable, contradicting its own UI copy. Fixed by adding `AVSpeechSynthesisVoiceQuality.isSiriQuality` and filtering on it in `availableVoices`, added an empty-state prompt in Settings for devices with no Enhanced/Premium voice installed, and pointed `SpeechService.bestAvailableVoice` through the same filtered pool (kept one invisible last-resort fallback to an installed voice so Read Aloud can't go fully silent). Inbox: reverted the 0.4.0 `LazyVGrid` gallery to a `List` of feed rows (favicon/name/count, matching `ManageFeedsView.feedRow`'s existing style) per direct user feedback on the card layout — same `InboxArticleListView` drill-in, same destinations, only the landing presentation changed. Self-implemented both (no dedicated native agent, and TTS required the kind of "verify against the real catalog first" rigor that's hard to hand off as a brief). Also fixed a real bug in `native/deploy.sh` found live: its device-detection only matched devicectl's `connected` state, but the paired iPhone was showing `available (paired)` (apparently unlocked on the user's end) and the strict filter rejected it twice before a direct `devicectl install` attempt proved the state text was stale/unreliable — broadened the match. Verified: `swift test` 39/39, `xcodebuild` clean on iOS device + Simulator. Bumped `MARKETING_VERSION` 0.4.0→0.4.1, installed to the paired iPhone | `native/FeedboxCore/Sources/FeedboxCore/Models/SpeechPreferences.swift`, `native/FeedboxCore/Sources/FeedboxCore/Services/SpeechService.swift`, `native/Feedbox/Views/SettingsView.swift`, `native/Feedbox/Views/InboxView.swift`, `native/deploy.sh`, `native/project.yml` | — |
| 2026-08-19 | native v0.5.0 (build verified, not installed) | Feature | User asked for 3 things in one message, the last 2 arriving mid-turn while I was still investigating the first: (1) center the first-screen menu and remove unneeded UI, (2) TTS should start at the article body only, skip other text, (3) clean up the reader body (Instapaper-inspired), bigger summary font, brief/detailed/bulleted-list AI summary options, and read-aloud-on-text-selection. Read `NavigationShell`/`AppSection` first — confirmed the sidebar `List` genuinely is the first screen on iPhone (`NavigationSplitView` collapses to it). Rebuilt it as a centered menu (icon badge/name/subtitle rows), moved Settings out to a toolbar gear icon since CLAUDE.md's own priority order doesn't list it as a "primary screen" — a judgment call, not something the user specified, flagged in the report. For TTS, verified independently (again) that no literal "Siri" voice exists before assuming what "start at the body" required — traced the actual bug: `SpeechService` dropped its title prefix easily, but the harder, real fix was that `ttsHighlightScript`'s word-wrapping walked the *entire* `document.body` (title/byline/AI-summary card included), while `SpeechService.plainText` was always body-only — the two were only ever coincidentally close to aligned. Fixed by wrapping the article body in a new `#fb-article-body` container and scoping the JS word-wrapper to it. For AI summaries, asked 2 clarifying questions before writing code since the data-model shape depended on the answer (3 distinct styles, chosen fresh each time, not a standing preference) — then rebuilt the summary bridge: collapsed 3 separate JS render functions (each independently escaping AI text) into one generic `setSummaryHTML(html)` injector, with a new `SummaryCardHTML.swift` as the *only* place that turns AI text into markup for both the initial page render and live updates; bulleted-style output is parsed from a constrained plain-text convention and escaped before Feedbox's own `<strong>`/`<li>` tags are added around it, never trusting AI-authored markup directly — same escaping discipline this codebase has insisted on since the 2026-08-07 XSS closure. Added a "Highlight"+speaker 2-button popover on text selection for the new per-passage read-aloud. Self-implemented throughout (no dedicated native agent; design-judgment-heavy). Caught and fixed 2 real bugs via the build (not just review): a duplicate `onSummarize` property declaration left over from a partial edit, and an extraneous closing brace. Also hit the known "new Swift files need `xcodegen generate` before they compile" gotcha again on the new `SummaryCardHTML.swift`. Verified: `swift test` 39/39, `xcodebuild` clean on iOS device destination (macOS build still fails on the same pre-existing, unrelated `.bottomBar`-on-macOS issue flagged last entry, now also visible in the refactored `InboxArticleListView.swift` — not a new regression, still out of scope). Bumped `MARKETING_VERSION` 0.4.1→0.5.0. Device install not completed — `devicectl` showed the paired iPhone as `unavailable` this session; user chose to skip rather than wait | `native/Feedbox/Views/NavigationShell.swift`, `native/Feedbox/Views/AppSection.swift`, `native/FeedboxCore/Sources/FeedboxCore/Services/SpeechService.swift`, `native/Feedbox/Views/ReaderWebView.swift`, `native/Feedbox/Views/ReaderView.swift`, `native/Feedbox/Views/SummaryCardHTML.swift` (new), `native/FeedboxCore/Sources/FeedboxCore/Models/SummaryStyle.swift` (new), `native/FeedboxCore/Sources/FeedboxCore/Models/ArticleSummary.swift`, `native/FeedboxCore/Sources/FeedboxCore/Services/AIService.swift`, `native/project.yml` | — |
| 2026-08-19 | native v0.5.1 (installed on iPhone + macOS) | Fix | User asked to "deploy updates to my iphone and desktop version" — iPhone was straightforward (`native/deploy.sh` against the now-connected paired device, installed 0.5.0/build 12 first). "Desktop" resolved to the native macOS target (this app's `project.yml` already declares `supportedDestinations: [iOS, macOS]`, not the web app, which was already fully deployed at v1.46.504 with a clean working tree) — confirmed the pre-existing macOS build failure flagged in the last two log entries was still blocking: `ManageFeedsView.swift`/`InboxArticleListView.swift` used `EditButton()` and `.bottomBar` toolbar placement, both iOS-only APIs. Fixed by wrapping both in `#if os(iOS)`, with a `.status`-placement equivalent of the Saved/Unread/All filter row for macOS (`EditButton` has no macOS equivalent — dropped there, list editing still reachable via existing swipe/context-menu actions, flagged as a minor parity gap not asked to fix). Verified `xcodebuild` clean on both `platform=iOS` (device) and `platform=macOS` before bumping, then `swift test` 39/39. Bumped `MARKETING_VERSION` 0.5.0→0.5.1 (build 12→13) and installed to both: `native/deploy.sh` to the paired iPhone, and a direct `xcodebuild`+`open` for macOS (no deploy script exists for that target yet — quit any running instance first so the relaunch picks up the new build rather than silently reusing a stale process) | `native/Feedbox/Views/ManageFeedsView.swift`, `native/Feedbox/Views/InboxArticleListView.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.5.2 (installed on iPhone + macOS) | Fix | User reported the just-deployed iPhone build "looks great, but ... functional buttons and views are not going anywhere or doing anything" — i.e. the whole app past the front menu was unreachable on iPhone specifically. Read `NavigationShell.swift` (the v0.5.0 first-screen-menu rewrite) and identified the root cause without needing device-automation to reproduce: `NavigationSplitView` only auto-pushes to its detail column on compact width (iPhone) when the sidebar drives selection through `List(selection:)` — a documented quirk tied specifically to that API, not to `NavigationSplitView`/selection state in general. v0.5.0 replaced the List with a plain custom `Button` mutating a bare `@State` (to get a chrome-free centered look), which never triggers that push — every menu tap silently updated state with no visible navigation. iPad/Mac (regular width, both columns always visible side-by-side) never needed the push and so weren't affected, matching the user's "iOS ... but" framing exactly. Confirmed no other screen had the same anti-pattern (`grep` across `Feedbox/Views/` showed every other screen already uses proper `NavigationStack`/`NavigationLink(value:)`). Fixed by branching `NavigationShell.body` on `\.horizontalSizeClass`: compact width now wraps the same `sidebar` view in a `NavigationStack` with `.navigationDestination(item: $selection)` (pushes off any state change, not List-specific, and gives a free back button); regular width keeps the untouched `NavigationSplitView`. Visual design identical on both — only the navigation container changed. Verified `xcodebuild` clean on `platform=iOS` (paired device) and `platform=macOS` before bumping, `swift test` 39/39. Bumped `MARKETING_VERSION` 0.5.1→0.5.2 (build 13→14), redeployed to both via `native/deploy.sh` (iPhone) and direct `xcodebuild`+`open` (macOS, quitting the stale running instance first) | `native/Feedbox/Views/NavigationShell.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.6.0 (installed on iPhone + macOS) | Feature | User asked to work on "the highlights fee[d]" (read as Cards, native's highlight-browsing screen — confirmed against CLAUDE.md's product vision language, "Browse by theme in Cards — not a list of articles but a map of ideas") and to make the Saved/Unread/All bottom nav "minimal ... a nice quick little switch button." Read the web app's `CardsPage.jsx` first to match its bucket-grid visual language rather than invent a new one. Rewrote `CardsView.swift`: bucket view is now a `LazyVGrid` of theme tiles (count as primary visual, a due-for-review badge computed with the same `isDue` calendar-day logic as `ReviewView`, an italic preview quote, a color bar hashed per theme name from existing palette tokens since the design system has no arbitrary per-tag colors — deliberately reused `accent`/`green`/`amber`/`blue`/`purple` rather than adding new hex values); tapping a tile pushes a per-theme `ThemeCardListView` of "notecard" rows (bordered `palette.card` panel, italic passage, note/synthesis, tag pills) via a second `.navigationDestination(for: String.self)` chained on the same `NavigationStack` alongside the existing `Highlight.self` one — verified this 2-level chained-destination pattern is standard/supported before relying on it. Deliberately kept the notecard rows inside a `List` (with `.listRowBackground(.clear)` + hidden separators to fake the floating-card look) rather than a hand-rolled `ScrollView`/`LazyVStack`, specifically because `.swipeActions` (the existing swipe-to-tag gesture) only works inside `List` — a lesson pulled forward from the same day's `NavigationSplitView`/`List(selection:)` investigation, that SwiftUI containers carry non-obvious built-in behavior a hand-rolled equivalent silently drops. Separately, collapsed the Saved/Unread/All filter — previously 3 always-visible icon buttons, independently duplicated (near-identically) in both `ManageFeedsView.swift` and `InboxArticleListView.swift` — into one shared `ArticleFilterMode` enum + `FilterSwitchButton` (new `Feedbox/Views/ArticleFilterMode.swift`), a single minimal pill that cycles all 3 modes on one tap, used identically by both screens; deleted both old private per-file enums and `filterBarButton` functions. Verified `xcodebuild` clean on `platform=iOS` (paired device) and `platform=macOS`, `swift test` 39/39, before bumping `MARKETING_VERSION` 0.5.2→0.6.0 (build 14→15, minor since it's new-looking UI, not just a fix) and redeploying to both: `native/deploy.sh` (iPhone) and direct `xcodebuild`+`open` (macOS) | `native/Feedbox/Views/CardsView.swift` (rewritten), `native/Feedbox/Views/ArticleFilterMode.swift` (new), `native/Feedbox/Views/ManageFeedsView.swift`, `native/Feedbox/Views/InboxArticleListView.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.6.1 (installed on iPhone + macOS) | Fix | User reported "the index fee[d]" (read as Cards again, per CLAUDE.md's "index system" language) "doesn't work ... not seeing anything ... not on the application," plus the bottom nav not showing — i.e. a regression in the v0.6.0 Cards work just shipped. Found the real bug on inspection, not guesswork: `InboxView`, `CardsView`, `ReviewView`, and `TodayView` each self-wrap their content in `NavigationStack` (needed so `NavigationSplitView`'s detail column gets independent push/pop on iPad/Mac) — but the same day's earlier `NavigationShell` compact-width fix (v0.5.2) *also* wraps its content in a `NavigationStack` for the iPhone push-to-detail fix. Net effect on iPhone: a `NavigationStack` nested inside another, which SwiftUI documents as unsupported — it silently breaks `.toolbar` placement (explaining the missing bottom filter bar) and can break destination pushes/rendering (explaining Cards appearing empty), while iPad/Mac's `NavigationSplitView` renders the detail column directly with no such collision, so those platforms were never affected — exactly matching the user's report being iPhone-specific. Fixed by removing the self-owned `NavigationStack` from all four screens (now plain content, pushed into whatever single stack the container provides) and having `NavigationShell` wrap one fresh `NavigationStack` around just the `NavigationSplitView` detail column at regular width, so every branch ends up with exactly one stack, never zero or two. Also hardened `native/deploy.sh` to auto-launch the app after install (`devicectl device process launch`) rather than leaving the user to find it manually, to close off "testing a stale running copy" as a future confusion vector (auto-launch itself failed silently on this device/signing setup — fell back to its warning message, not a blocker). Verified `xcodebuild` clean on `platform=iOS` (paired device) and `platform=macOS`, `swift test` 39/39. Bumped `MARKETING_VERSION` 0.6.0→0.6.1 (build 15→16), redeployed to both via `native/deploy.sh` and `native/deploy-macos.sh` | `native/Feedbox/Views/InboxView.swift`, `native/Feedbox/Views/CardsView.swift`, `native/Feedbox/Views/ReviewView.swift`, `native/Feedbox/Views/TodayView.swift`, `native/Feedbox/Views/NavigationShell.swift`, `native/deploy.sh`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.6.2 (installed on iPhone + macOS) | Security | `/orchestrate` run: user asked for a security audit of the native app. Decomposed into 4 disjoint, parallel read-only investigations (auth/session, WebView/content-injection, local data/OPML, app config/network/supply chain) dispatched to `general-purpose` Sonnet-tier agents — no dedicated native or security agent exists in `.claude/agents/`, and CLAUDE.md's own routing table rules out Haiku for judgment-heavy, non-mechanically-checkable work. Judged adversarially: independently re-verified every claimed finding by reading the actual source myself before accepting any of them (grepped for `WKNavigationDelegate` to confirm its absence, read `OPMLImportService.swift`'s parser config to confirm no XXE delegate method, read `AuthSession.swift`/`SupabaseBridge.swift`/`FeedboxApp.swift` line-by-line against the auth-hijack claim, etc.) — all 4 reports held up, nothing hallucinated. Found 1 Critical (reflected XSS: `ReaderView.swift` spliced `content.title`/`.byline` into the reader's HTML unescaped, bypassing the `SwiftSoup Whitelist.relaxed()` cleaner that only ever covered `bodyHTML`) and 1 High (`FeedboxApp.onOpenURL` accepted any `feedbox://...#access_token=...` URL with zero validation — no `state`/nonce tying a callback to a sign-in the app actually initiated, letting a crafted link log a victim into an attacker's Supabase account) plus 4 Medium/Low items (no `WKNavigationDelegate` — in-app phishing via article links; sign-out not revoking server-side; unbounded OPML XML entity expansion; no macOS App Sandbox) deferred to a follow-up pass per the user's "fix the top two, everything else later" call. Per CLAUDE.md's routing rule that security-sensitive code is escalate/self-implement (not delegated further), fixed both myself: title/byline (and hero-image `src`, a related Low finding, while in the file) now go through the existing `.htmlEscaped` helper; added `AuthSession.beginPendingAuth()`/`consumePendingAuth(matching:)` (a single-use, TTL-bounded anti-CSRF nonce stored in UserDefaults — survives the app being killed while the user opens a magic-link email) threaded through `redirectTo`'s query string on both the magic-link and GitHub OAuth paths, validated before any callback's tokens are trusted; `SupabaseBridgeService.parseCallback` now also extracts `state`. Added 2 new tests for `parseCallback`'s `state` extraction (deliberately did NOT add tests for the UserDefaults-touching `AuthSession` instance methods, matching this codebase's existing convention of only unit-testing pure functions, not real-Keychain/UserDefaults-touching ones). Flagged clearly in the changelog and to the user that the `state` query param's compatibility with Supabase's redirect allow-list is expected-safe by OAuth-2.0 convention but not verified live (no way to complete a real magic-link/GitHub round-trip as an agent) — needs a manual sign-in test. Verified `swift test` 41/41, `xcodebuild` clean on `platform=iOS` (paired device) and `platform=macOS`. Bumped `MARKETING_VERSION` 0.6.1→0.6.2 (build 16→17); iPhone install hit a transient `CoreDeviceError 3002` ("Connection interrupted", wireless-tunnel flake, not code-related) twice before a third `native/deploy.sh` run succeeded — macOS via `native/deploy-macos.sh` succeeded on the first try | `native/Feedbox/Views/ReaderView.swift`, `native/Feedbox/FeedboxApp.swift`, `native/Feedbox/Views/SignInView.swift`, `native/FeedboxCore/Sources/FeedboxCore/Services/AuthSession.swift`, `native/FeedboxCore/Sources/FeedboxCore/Services/SupabaseBridge.swift`, `native/FeedboxCore/Tests/FeedboxCoreTests/SupabaseBridgeServiceTests.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.7.0 (installed on iPhone + macOS) | Feature | User asked to turn the Today screen into "Discovery" — popular RSS feeds organized by topic, subscribable in-app, plus top stories of the day. Confirmed scope via `AskUserQuestion` before touching `AppSection`/nav (a shared, primary-screen-defining enum): full replacement of Today (dropping the streak/reads-today stats) vs. keeping stats visible vs. adding a 6th tab — user chose full replacement; and top-stories source — user chose live-fetched from the curated directory over the user's own subscriptions. Read `RSSService`/`FeedSource` first and confirmed `fetchItems`/`fetchAll` already take a plain `FeedSource` value, not a persisted SwiftData `Feed` — meaning "fetch articles from feeds the user hasn't subscribed to yet" needed no new fetch code, just constructing `FeedSource`s from the new directory. Built the curated list myself (`FeedboxCore/Models/FeedDirectory.swift`: `DirectoryTopic` enum, 8 topics — Tech/News/Business/Science/Sports/Gaming/Culture/Food — and 26 `DirectoryFeed` entries) since no feed-search API exists; spot-checked every URL live with `curl` before committing to it, swapping 4 that 404'd/timed out (Fast Company Design, Polygon, Core77, Pitchfork, HBR, Serious Eats) for verified-200 alternatives. New `Feedbox/Views/DiscoverView.swift` replaces `TodayView.swift`: per-topic horizontal-scroll rows of feed cards with a one-tap Subscribe button (checks `@Query`-fetched `Feed.url`s to show "Subscribed" and disable re-adding), plus a bottom "Top Stories" rail that concurrently fetches one spotlight feed per topic via the existing `RSSService().fetchAll`, sorts by `publishedAt` descending, and pushes into the existing `ReaderView` via the same `FeedItem` navigation destination pattern `InboxArticleListView` already uses. Renamed `AppSection.today` → `.discover` throughout (`NavigationShell`'s menu list and switch, doc comments referencing the product-vision screen order) rather than leave a stale case name. No new dependencies. Verified `swift test` 41/41, `xcodebuild` clean on `platform=macOS` and `platform=iOS` (paired device) before bumping. Bumped `MARKETING_VERSION` 0.6.2→0.7.0 (build 17→18, minor since it replaces a primary screen with new user-facing capability), deployed to both via `native/deploy.sh` (iPhone) and `native/deploy-macos.sh`. Also committed the previously-unstaged `AGENT_LOG.md` entries from earlier in the day — `native/` itself is gitignored (confirmed via `git check-ignore`) so none of today's native work needed or received a git commit before now | `native/FeedboxCore/Sources/FeedboxCore/Models/FeedDirectory.swift` (new), `native/Feedbox/Views/DiscoverView.swift` (new), `native/Feedbox/Views/TodayView.swift` (deleted), `native/Feedbox/Views/AppSection.swift`, `native/Feedbox/Views/NavigationShell.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.7.1 (installed on iPhone + macOS) | Fix | User reported the app "seems unstable and lots of bugs" after the Discover deploy — confirmed via `AskUserQuestion` this meant crashes and freezes specifically on the new Discover screen before chasing anything. Found real evidence rather than guessing: `~/Library/Logs/DiagnosticReports/Feedbox-2026-08-19-174037.ips` had synced from the macOS build automatically, and symbolicated to `OAuthSession.authorize`'s `ASWebAuthenticationSession` completion closure hitting `dispatch_assert_queue_fail`/`swift_task_checkIsolatedSwift` — a genuine, pre-existing (not caused by today's Discover work) Swift 6 actor-isolation crash: the completion closure, written inline inside the `@MainActor`-isolated `authorize` method, was inferred `@MainActor`-isolated by the compiler despite being passed to a `@Sendable`-typed parameter, but `ASWebAuthenticationSession` actually invokes it off the main thread (confirmed in the trace via an XPC reply callout) — the runtime's isolation check trapped the moment that happened. Fixed by binding the completion handler to an explicit `@Sendable`-typed `let` first, forcing correct non-isolated inference regardless of lexical context. Separately, self-reviewed the new `DiscoverView.swift`/`RSSService.swift` for the Discover-specific freeze report and found two real issues: (1) `RSSService.fetchItems` had no per-request timeout — `FeedKit.Feed(remoteURL:)` falls back to `URLSession`'s 60s default, and since `fetchAll` awaits every source before returning, Discover's 8-feeds-concurrently-on-every-visit "Top Stories" fetch could stall for up to a minute on one slow/dead feed; added a 10s timeout via a `withThrowingTaskGroup` race so a timed-out feed just drops through the existing per-source failure path instead of blocking the rest; (2) `DiscoverView`'s topic rows and Top Stories rail used plain `VStack`/`HStack` instead of `LazyVStack`/`LazyHStack`, forcing all 8 topic sections, 26 feed cards, and 12 `AsyncImage`-backed story cards to build eagerly on first appearance — switched to the lazy variants. Could not live-repro either fix against the actual crash/freeze (no way to trigger a GitHub OAuth round-trip or force a slow feed as an agent), so flagged to the user that a manual re-test of both GitHub sign-in and Discover is the real confirmation, not just a clean build. Verified `swift test` 41/41, `xcodebuild` clean on `platform=macOS` and `platform=iOS` (paired device). Bumped `MARKETING_VERSION` 0.7.0→0.7.1 (build 18→19), redeployed to both via `native/deploy.sh` and `native/deploy-macos.sh` | `native/FeedboxCore/Sources/FeedboxCore/Services/OAuthSession.swift`, `native/FeedboxCore/Sources/FeedboxCore/Services/RSSService.swift`, `native/Feedbox/Views/DiscoverView.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.7.2 (installed on iPhone + macOS) | Fix | User reported Discover was "still buggy" after v0.7.1 and asked for "a complete revamp" plus a smaller unrelated ask (center the AI summary heading). Confirmed via `AskUserQuestion` before rebuilding anything: same crashes/freezes persisting (not a new/different symptom), revamp scope covering the whole native app rather than just Discover, and that only the summary card's "AI SUMMARY" eyebrow label should center, not its body text. Checked for a fresh crash log first rather than guessing blind — none had synced since the 0.7.1 install (`~/Library/Logs/DiagnosticReports` unchanged, `log show --predicate 'process == "Feedbox"'` over the last 2h empty) — so worked from the app's own history instead: `grep -rn "navigationDestination"` across `Feedbox/Views/` turned up `.navigationDestination(for: FeedItem.self)` registered independently in *both* `InboxArticleListView` and `DiscoverView`, the same class of bug (duplicate/conflicting destination registration in one `NavigationStack`) behind this app's two earlier NavigationStack crashes today (v0.5.2's `List(selection:)` push bug, v0.6.1's nested-stack bug) — a highly plausible real cause given the pattern, even without a fresh crash trace naming it directly. Fixed by hoisting a single `FeedItem → ReaderView` registration to `NavigationShell`'s stack root (both the compact-width and regular-width `NavigationStack`s) and removing both per-screen duplicates. While in `DiscoverView`, also deduped "Top Stories" by article link before capping to 12 — two spotlight feeds occasionally syndicate the same story, and duplicate `Identifiable` ids in a `ForEach` is undefined behavior, a plausible secondary contributor. Interpreted "whole app revamp" pragmatically as hardening the actual fragile layer (navigation-destination architecture) rather than a ground-up visual rewrite of every screen, since the user's stated problem was stability, not aesthetics — flagged this scoping call to the user rather than silently deciding it. Separately, centered `.fb-summary-eyebrow` (`justify-content: center` added to its existing `display: flex` row) in `ReaderView.swift`'s embedded CSS; `.fb-summary-text` (the body) left untouched per the user's answer. Again could not live-repro the navigation crash myself to confirm the fix holds — flagged that a manual retest of Discover (tapping into a Top Story, and switching between Inbox/Discover a few times) is the real confirmation. Verified `swift test` 41/41, `xcodebuild` clean on `platform=macOS` and `platform=iOS` (paired device). Bumped `MARKETING_VERSION` 0.7.1→0.7.2 (build 19→20), redeployed to both via `native/deploy.sh` and `native/deploy-macos.sh` | `native/Feedbox/Views/NavigationShell.swift`, `native/Feedbox/Views/InboxArticleListView.swift`, `native/Feedbox/Views/DiscoverView.swift`, `native/Feedbox/Views/ReaderView.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.8.0 (installed on iPhone + macOS) | Feature | User asked to "completely rebuild the inbox." Confirmed scope via `AskUserQuestion` before touching it: native app (not web), and a visual/UX redesign rather than a stability pass (Inbox's stability piece — the duplicate `FeedItem` navigation destination — was already fixed as part of the previous v0.7.2 entry). No specific design direction was given, so followed this session's established default: read the web app's actual Inbox (`InboxPage.jsx` — grepped its structure rather than reading all 2158 lines, given the token-efficiency rule) to match existing product design instead of inventing a new one. Found the web Inbox is a unified, date/feed-grouped article stream with a sources sidebar for filtering — native's Inbox was structurally different: a feed-picker landing page requiring a tap into a feed (or "All Items") before seeing any article, an extra step the web app doesn't have. Rebuilt to match: `InboxView` now embeds the stream directly (defaults to every feed), with a new "Sources" sheet replacing the old per-feed navigation push — tapping a source sets `feedFilter` in place and dismisses, with a checkmark on the active source. Renamed `InboxArticleListView` → `InboxStreamView` and stripped its self-owned navigation chrome (title/toolbar/search/toolbar-owned sort-group-filter controls all moved up to `InboxView`, the new screen's sole navigation owner) since it's now an embedded content view, not a pushed screen — `searchText`/`filterMode` became `@Binding`s owned by `InboxView` instead of local `@State`. Used `.id(feedFilter?.id)` on the embedded stream so switching sources resets scroll-tracking/mark-read-on-scroll state cleanly, and explicitly cleared `searchText` on every source switch (deliberate: search context is feed-specific, but the Saved/Unread/All filter mode is a general reading preference and was left to persist across the switch). Moved "Add Feed" from the main toolbar into the Sources sheet (as a nested sheet-on-sheet, verified supported) next to the list it manages, decluttering the main toolbar down to Sources/Sort-Group/Refresh. All existing behavior — search, sort/group, the shared `FilterSwitchButton`, swipe actions, mark-read-on-scroll, the failed-feeds banner — carried over via direct reuse of `InboxStreamView`'s logic, not reimplemented. Verified `swift test` 41/41 (untouched — no `FeedboxCore` package logic changed), `xcodebuild` clean on `platform=macOS` and `platform=iOS` (paired device), and grepped for any other reference to the deleted `InboxArticleListView`/`InboxDestination` before deleting the old file. Bumped `MARKETING_VERSION` 0.7.2→0.8.0 (build 20→21, minor: new-looking primary-screen architecture, not just a fix), deployed to both via `native/deploy.sh` and `native/deploy-macos.sh` | `native/Feedbox/Views/InboxView.swift` (rewritten), `native/Feedbox/Views/InboxStreamView.swift` (new, replaces `InboxArticleListView.swift`), `native/Feedbox/Views/ArticleFilterMode.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.9.0 (installed on iPhone + macOS) | Feature | User asked to rework Discover: "top news stories on the top like a flip board magazine style," then category rows that "open up into cards with rss feeds to subscribe." Specific enough to build directly without another clarifying round. Replaced the small horizontal-scroll "Top Stories" cards with a `TabView` + `.page` style paged hero carousel — full-bleed article art (`AsyncImage`, fallback to a hashed-color gradient placeholder for image-less items via a new `nameAccent` helper keyed on feed name, since hero cards have no `DirectoryTopic` in scope to key `topicAccent` off of), a bottom `LinearGradient` for legibility, and headline/source text overlaid directly on the image — Flipboard's actual layout, not a thumbnail-plus-caption row. `#if os(iOS)` gates `.tabViewStyle(.page(...))`/`.indexViewStyle(...)` since neither exists on macOS; macOS falls back to `.automatic` (plain tab bar). Collapsed the old "all 8 topics shown at once as horizontal scrolls" section into a single tappable "Categories" list (icon, title, feed count, subscribed count, chevron) — tapping pushes a new `TopicFeedsView` (new file), a dedicated `LazyVGrid` card grid for that topic's feeds with the same one-tap Subscribe button Discover had inline before. Moved subscribe state/logic (`isSubscribed`/`subscribe`, the `@Environment(\.modelContext)`) out of `DiscoverView` into `TopicFeedsView` accordingly, since subscribing only happens after opening a category now. Made `topicAccent` non-`private` (was scoped to `DiscoverView`) so `TopicFeedsView` can reuse the identical hash — a topic's color now stays consistent between its category row and its feed cards, rather than each file inventing its own. Added `Hashable` to `DirectoryTopic`'s conformance list (needed for `.navigationDestination(for: DirectoryTopic.self)`) — free via auto-synthesis since it's a raw-value enum with no associated values. Registered that destination locally in `DiscoverView` (not hoisted to `NavigationShell`, unlike `FeedItem`) since `DiscoverView` is the only screen that ever pushes a `DirectoryTopic` — no duplicate-registration risk to guard against here. Verified `swift test` 41/41 (untouched), `xcodebuild` clean on `platform=macOS` and `platform=iOS` (paired device). Bumped `MARKETING_VERSION` 0.8.0→0.9.0 (build 21→22), deployed to both via `native/deploy.sh` and `native/deploy-macos.sh` | `native/Feedbox/Views/DiscoverView.swift` (rewritten), `native/Feedbox/Views/TopicFeedsView.swift` (new), `native/FeedboxCore/Sources/FeedboxCore/Models/FeedDirectory.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.9.1 (installed on iPhone + macOS) | Fix | User asked to fine-tune Inbox: article images missing for some feeds, and rebuild the Starred/Unread/All filter into "three symbols" where only the tapped one shows text. For the image gap, checked real feed XML with `curl` before writing any fix rather than guessing — TechCrunch, Fortune, and ESPN's actual RSS items genuinely contain zero image data (no `media:`, no `enclosure`, no inline `<img>`), confirmed by fetching and inspecting their raw item blocks; not fixable without an extra full-page fetch per article (out of scope, flagged to user rather than silently building it). Also confirmed via `InboxPreferences.init`'s defaults (`displayFavicons = true`) that the favicon fallback for image-less items was already working as designed, not broken. Did find and fix 3 real extraction gaps in `RSSService` for feeds that *do* embed an image but weren't being caught: entity-double-encoded HTML (a literal `&lt;img&gt;`) wasn't recognized as a tag by the HTML parser — added `decodeCommonEntities` before `firstImageSrc`'s `SwiftSoup.parse`, mirroring the web app's existing `decodeHtmlEntities` fix for the same underlying feed-quality issue (see top-level `CHANGELOG.md` v1.46.503); enclosures with no `type` attribute were dropped even when the URL was clearly an image — added a file-extension sniff fallback, gated to only fire when `type` is absent (never overrides an explicit non-image type); `media:content` with neither `medium` nor `type` set was dropped — added as a last-resort tier, excluding anything that looks like audio/video. Separately rebuilt `FilterSwitchButton` (shared by `InboxView` and `ManageFeedsView`) from a single cycling pill into 3 always-visible icon buttons — tapping one makes it active and expands it to show its label, the other two stay icon-only; renamed the "Saved" filter's label to "Starred" (its icon was already `star.fill`) per the user's own wording, updating `ManageFeedsView`'s matching `headerLabel` switch too so the two screens' terminology doesn't drift apart. Verified `swift test` 41/41, `xcodebuild` clean on `platform=macOS` and `platform=iOS` (paired device). Bumped `MARKETING_VERSION` 0.9.0→0.9.1 (build 22→23), deployed to both via `native/deploy.sh` and `native/deploy-macos.sh` | `native/FeedboxCore/Sources/FeedboxCore/Services/RSSService.swift`, `native/Feedbox/Views/ArticleFilterMode.swift`, `native/Feedbox/Views/ManageFeedsView.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
| 2026-08-19 | native v0.9.2 (installed on iPhone + macOS) | Fix | User reported the just-rebuilt Starred/Unread/All filter needed to be smaller, had a visible border, and showed no text on the active button. Diagnosed rather than guessed: the control lived in `ToolbarItem(placement: .bottomBar)` (iOS) / `.status` (macOS), and this project's `project.yml` declares `deploymentTarget.iOS: "26.0"` — iOS 26's toolbar rendering automatically wraps each `Button` placed via `ToolbarItem` in its own glass pill/border, which is the border the user saw (nothing in `FilterSwitchButton`'s own code drew a stroke), and toolbar items don't reliably resize their bounding box when child content grows, which plausibly clipped the active button's label the moment it appeared. Fixed by moving the control out of the toolbar entirely, into a `.safeAreaInset(edge: .bottom)` view on both `InboxView` and `ManageFeedsView` (which share `FilterSwitchButton`) — a plain view outside the toolbar isn't subject to its automatic per-button chrome or its sizing behavior, so both the border and the clipped label are addressed by the same move rather than two separate patches. Backed the new bar with `.ultraThinMaterial` so it still reads as a bottom bar, not a floating control. Also shrank `FilterSwitchButton` itself per request: icon 13pt→11pt, tighter horizontal/vertical padding, smaller icon-label gap and inter-button spacing. Verified `swift test` 41/41 (untouched), `xcodebuild` clean on `platform=macOS` and `platform=iOS` (paired device). Bumped `MARKETING_VERSION` 0.9.1→0.9.2 (build 23→24), deployed to both via `native/deploy.sh` and `native/deploy-macos.sh` | `native/Feedbox/Views/InboxView.swift`, `native/Feedbox/Views/ManageFeedsView.swift`, `native/Feedbox/Views/ArticleFilterMode.swift`, `native/project.yml`, `native/CHANGELOG.md` | — |
