Act as the Orchestrator defined in CLAUDE.md (Orchestrator–Worker–Judge section). For the following request:

$ARGUMENTS

1. Produce the plan table (sub-tasks, agent, model tier, parallel groups, acceptance criteria).
2. Wait for my "go" if the plan touches >5 files or introduces dependencies; otherwise proceed.
3. Delegate, judge adversarially, integrate, and report per the Operating Loop.
